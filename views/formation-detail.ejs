<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><title>Module | FORFEO</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"></head>
<body class="bg-light">
    <nav class="navbar navbar-white bg-white border-bottom py-3">
        <div class="container">
            <a class="btn btn-sm btn-outline-primary rounded-pill" href="/formations"><i class="bi bi-arrow-left"></i> Retour à l'Académie</a>
            <a href="/" class="btn btn-sm btn-outline-secondary rounded-pill ms-2"><i class="bi bi-house-door"></i> Accueil</a>
        </div>
    </nav>
    <div class="container py-5">
        <div class="ratio ratio-16x9 mb-5 rounded-4 shadow overflow-hidden"><iframe src="<%= module.video_url %>" allowfullscreen></iframe></div>
        <% if (userScore.tentatives >= 3 && userScore.meilleur_score < 12) { %>
            <div class="alert alert-danger p-5 text-center rounded-4 shadow">🚨 3 échecs consécutifs. Contactez votre employeur.</div>
        <% } else { %>
            <div class="card p-5 border-0 shadow-sm rounded-4 text-center">
                <h3 class="fw-bold mb-4">Examen : <%= module.titre %></h3>
                <form action="/formations/soumettre-quizz" method="POST">
                    <input type="hidden" name="module_id" value="<%= module.id %>">
                    <input type="hidden" name="score_obtenu" value="13"> <button type="submit" class="btn btn-success btn-lg rounded-pill w-100 fw-bold shadow">Soumettre mes réponses</button>
                </form>
            </div>
        <% } %>
    </div>
</body>
</html>
