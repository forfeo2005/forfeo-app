<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Examen | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="container py-5" style="max-width: 800px;">
        <a href="/employe/dashboard" class="btn btn-outline-secondary btn-sm mb-4 rounded-pill">← Retour</a>
        
        <div class="glass-card">
            <div class="text-center mb-5">
                <h2 class="fw-bold text-primary"><%= module.titre %></h2>
                <p>Répondez aux 15 questions. 80% requis pour réussir.</p>
            </div>

            <form action="/formations/soumettre-quizz" method="POST">
                <input type="hidden" name="module_id" value="<%= module.id %>">
                
                <% questions.forEach((q, index) => { %>
                    <div class="mb-4 p-3 border rounded bg-white">
                        <h6 class="fw-bold mb-3"><%= index + 1 %>. <%= q.question %></h6>
                        
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="q<%= q.id %>" value="A" id="q<%= q.id %>a" required>
                            <label class="form-check-label" for="q<%= q.id %>a">A) <%= q.option_a %></label>
                        </div>
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="q<%= q.id %>" value="B" id="q<%= q.id %>b">
                            <label class="form-check-label" for="q<%= q.id %>b">B) <%= q.option_b %></label>
                        </div>
                        <div class="form-check mb-2">
                            <input class="form-check-input" type="radio" name="q<%= q.id %>" value="C" id="q<%= q.id %>c">
                            <label class="form-check-label" for="q<%= q.id %>c">C) <%= q.option_c %></label>
                        </div>
                    </div>
                <% }) %>

                <button class="btn btn-primary w-100 py-3 rounded-pill fw-bold">Soumettre mes réponses</button>
            </form>
        </div>
    </div>
</body>
</html>
