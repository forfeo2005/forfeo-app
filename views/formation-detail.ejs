<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%= module.titre %> | FORFEO Academy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7f6; font-family: 'Inter', sans-serif; }
        .video-wrapper { border-radius: 20px; overflow: hidden; box-shadow: 0 15px 35px rgba(0,0,0,0.1); background: #000; }
        .quiz-container { border: none; border-radius: 25px; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-light bg-white border-bottom py-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/formations"><i class="bi bi-arrow-left"></i> Retour</a>
            <span class="fw-bold"><%= module.titre %></span>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1">
        <% if (userScore.tentatives >= 3 && userScore.meilleur_score < 12) { %>
            <div class="card p-5 text-center border-0 shadow-sm rounded-4">
                <i class="bi bi-lock-fill text-danger display-1"></i>
                <h2 class="fw-bold mt-3">Tentatives épuisées</h2>
                <p class="text-muted">Vous avez échoué à vos 3 tentatives (Note < 80%). <br> Contactez votre employeur pour débloquer l'accès.</p>
            </div>
        <% } else { %>
            <div class="video-wrapper mb-5 ratio ratio-16x9">
                <iframe src="<%= module.video_url %>" allowfullscreen></iframe>
            </div>

            <div class="card quiz-container shadow-sm p-4 p-md-5 bg-white">
                <h3 class="fw-bold mb-4">Examen Final (15 questions)</h3>
                <form action="/formations/soumettre-quizz" method="POST">
                    <input type="hidden" name="module_id" value="<%= module.id %>">
                    <% questions.forEach((q, i) => { %>
                        <div class="mb-4 p-4 bg-light rounded-4 border-start border-primary border-4">
                            <h6 class="fw-bold mb-3"><%= i+1 %>. <%= q.question %></h6>
                            <div class="form-check mb-2">
                                <input class="form-check-input" type="radio" name="q<%= q.id %>" value="A" required>
                                <label class="form-check-label"><%= q.option_a %></label>
                            </div>
                            <div class="form-check mb-2">
                                <input class="form-check-input" type="radio" name="q<%= q.id %>" value="B">
                                <label class="form-check-label"><%= q.option_b %></label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="q<%= q.id %>" value="C">
                                <label class="form-check-label"><%= q.option_c %></label>
                            </div>
                        </div>
                    <% }) %>
                    <button type="submit" class="btn btn-primary btn-lg w-100 rounded-pill fw-bold shadow">Soumettre mon examen</button>
                </form>
            </div>
        <% } %>
    </div>

    <footer class="bg-dark text-white py-4 mt-auto text-center">
        <p class="small text-white-50 m-0">FORFEO Academy © 2025 | Standard de formation conforme au Québec.</p>
    </footer>
</body>
</html>
