<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title><%= module.titre %> | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>.video-container { border-radius: 20px; overflow: hidden; background: #000; }</style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <% if (userScore.tentatives >= 3 && userScore.meilleur_score < 12) { %>
            <div class="alert alert-danger text-center p-5 rounded-4 shadow">
                <h3 class="fw-bold">🚨 Accès Verrouillé</h3>
                <p>Vous avez échoué à vos 3 tentatives. Contactez votre employeur pour une session de coaching.</p>
                <a href="/formations" class="btn btn-outline-danger rounded-pill px-4 mt-3">Retour</a>
            </div>
        <% } else { %>
            <div class="video-container shadow-lg mb-5 ratio ratio-16x9">
                <iframe src="<%= module.video_url %>" allowfullscreen></iframe>
            </div>
            <div class="card border-0 shadow-sm rounded-4 p-5">
                <h3 class="fw-bold mb-4">Examen Final : <%= module.titre %></h3>
                <p class="text-muted mb-5">Veuillez répondre aux 15 questions. Note de passage : 80% (12/15).</p>
                <form action="/formations/soumettre-quizz" method="POST">
                    <input type="hidden" name="module_id" value="<%= module.id %>">
                    <% questions.forEach((q, i) => { %>
                        <div class="mb-5 p-4 bg-light rounded-4">
                            <h6 class="fw-bold mb-3"><%= i+1 %>. <%= q.question %></h6>
                            <div class="form-check mb-2"><input class="form-check-input" type="radio" name="q<%= q.id %>" value="A" required> <%= q.option_a %></div>
                            <div class="form-check mb-2"><input class="form-check-input" type="radio" name="q<%= q.id %>" value="B"> <%= q.option_b %></div>
                            <div class="form-check"><input class="form-check-input" type="radio" name="q<%= q.id %>" value="C"> <%= q.option_c %></div>
                        </div>
                    <% }) %>
                    <button type="submit" class="btn btn-primary btn-lg rounded-pill w-100 shadow-sm fw-bold">Soumettre mon examen</button>
                </form>
            </div>
        <% } %>
    </div>
</body>
</html>
