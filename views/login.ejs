<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; font-family: 'Inter', sans-serif; display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
        .login-card { border-radius: 25px; border: none; box-shadow: 0 15px 35px rgba(0,0,0,0.1); background: white; padding: 40px; width: 100%; max-width: 450px; position: relative; }
        .btn-connect { background: linear-gradient(to right, #007bff, #00c6ff); border: none; border-radius: 12px; padding: 12px; font-weight: bold; width: 100%; color: white; transition: 0.3s; margin-top: 20px; }
        .btn-connect:hover { transform: translateY(-2px); opacity: 0.9; }
        .form-control { border-radius: 10px; padding: 12px 15px; background-color: #eef4ff; border: none; }
        .back-home { position: absolute; top: -50px; left: 0; text-decoration: none; color: #666; font-weight: 600; font-size: 0.9rem; transition: 0.3s; }
        .back-home:hover { color: #007bff; }
    </style>
</head>
<body>

    <div class="login-card">
        <a href="/" class="back-home">← Retour à l'accueil</a>

        <div class="text-center mb-4">
            <h2 class="fw-bold text-primary">FORFEO LAB</h2>
            <h5 class="fw-bold">Bon retour parmi nous</h5>
        </div>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label class="small fw-bold mb-2">Courriel</label>
                <input type="email" name="email" class="form-control" placeholder="mathieuyeo87@gmail.com" required>
            </div>

            <div class="mb-4">
                <label class="small fw-bold mb-2">Mot de passe</label>
                <input type="password" name="password" class="form-control" placeholder="••••••••" required>
            </div>

            <button type="submit" class="btn-connect text-uppercase shadow-sm">Se connecter</button>
        </form>

        <div class="text-center mt-4">
            <p class="small text-muted mb-1">Pas encore de compte ?</p>
            <div class="d-flex justify-content-center gap-2">
                <a href="/register?role=ambassadeur" class="small fw-bold text-primary text-decoration-none">Ambassadeur</a>
                <span class="text-muted">ou</span>
                <a href="/register?role=entreprise" class="small fw-bold text-primary text-decoration-none">Entreprise</a>
            </div>
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
