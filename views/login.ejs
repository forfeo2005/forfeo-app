<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5" style="max-width: 450px;">
        <div class="card p-4 shadow border-0 rounded-4">
            <h2 class="fw-bold text-center mb-4">Connexion</h2>
            <% if (msg) { %><div class="alert alert-info py-2 small"><%= msg %></div><% } %>
            <form action="/login" method="POST">
                <div class="mb-3">
                    <label class="form-label small fw-bold">COURRIEL</label>
                    <input type="email" name="email" class="form-control rounded-3" required>
                </div>
                <div class="mb-4">
                    <label class="form-label small fw-bold">MOT DE PASSE</label>
                    <input type="password" name="password" class="form-control rounded-3" required>
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold">Se connecter</button>
            </form>
            <p class="text-center mt-3 small">Pas de compte ? <a href="/register">S'inscrire</a></p>
        </div>
    </div>
    </body>
</html>
