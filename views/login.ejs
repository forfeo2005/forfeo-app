<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion Entreprise - FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">
    <div class="card border-0 shadow-lg p-4 p-md-5 rounded-4" style="max-width: 400px; width: 100%;">
        <div class="text-center mb-4">
            <h3 class="fw-bold text-dark">Espace Client</h3>
            <p class="text-muted small">Accédez à vos rapports et audits.</p>
        </div>
        <form id="loginForm">
            <div class="mb-3">
                <label class="form-label fw-bold small text-muted">ID Entreprise</label>
                <input type="text" id="businessId" class="form-control form-control-lg bg-light border-0" placeholder="ex: hotel-prestige" required>
            </div>
            <div class="mb-4">
                <label class="form-label fw-bold small text-muted">Mot de passe</label>
                <input type="password" class="form-control form-control-lg bg-light border-0" placeholder="••••••••" required>
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-dark btn-lg rounded-pill">Se connecter</button>
            </div>
        </form>
        <div class="text-center mt-4">
            <a href="/" class="text-decoration-none small text-muted">Retour au site public</a>
        </div>
    </div>
    <script>
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const businessId = document.getElementById('businessId').value;
            if(businessId) {
                localStorage.setItem('forfeo_business_logged_in', 'true');
                localStorage.setItem('forfeo_business_user', businessId);
                // Redirection via la route Node.js
                window.location.href = '/dashboard';
            }
        });
    </script>
    <script src="/js/forfy.js"></script>
</body>
</html>