<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Plan d'Amélioration Stratégique | Forfeo Lab</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #fff; color: #333; }
        .survey-container { max-width: 850px; margin: 40px auto; padding: 40px; border: 2px solid #000; border-radius: 15px; }
        .priority-group { display: flex; gap: 15px; margin: 20px 0; }
        .priority-option { flex: 1; padding: 15px; border: 1px solid #ddd; text-align: center; cursor: pointer; border-radius: 8px; font-weight: bold; }
        .priority-option:hover { background: #f0f0f0; }
        .selected-prio { background: #000 !important; color: #fff !important; border-color: #000 !important; }
        .score-input { font-size: 2rem; width: 120px; text-align: center; border: 2px solid #000; border-radius: 10px; padding: 10px; }
        .btn-final { background: #00aaff; color: #fff; padding: 20px; border: none; border-radius: 10px; width: 100%; font-size: 1.2rem; font-weight: bold; cursor: pointer; margin-top: 40px; }
    </style>
</head>
<body>
    <div class="survey-container">
        <h1>🚀 Recommandations d'Optimisation</h1>
        <p>Transformer vos observations en leviers de croissance pour l'entreprise.</p>

        <form action="/submit-survey" method="POST">
            <input type="hidden" name="survey_type" value="amelioration_pro">

            <label style="font-weight:bold; display:block; margin-top:30px;">Recommandation n°1 (Impact immédiat)</label>
            <textarea name="reco_1" style="width:100%; height:100px; margin-top:10px; padding:10px;" placeholder="Ex: Former le personnel à la technique de vente croisée..."></textarea>

            <label style="font-weight:bold; display:block; margin-top:30px;">Niveau d'Urgence Stratégique</label>
            <div class="priority-group">
                <div class="priority-option" onclick="selectPrio(this, 'Basse')">BASSE</div>
                <div class="priority-option selected-prio" onclick="selectPrio(this, 'Modérée')">MODÉRÉE</div>
                <div class="priority-option" onclick="selectPrio(this, 'Critique')">CRITIQUE 🔥</div>
            </div>
            <input type="hidden" name="urgence_niveau" id="urgence-input" value="Modérée">

            <div style="background: #f9f9f9; padding: 30px; border-radius: 12px; margin-top: 30px; text-align: center;">
                <label style="font-weight:bold; display:block; margin-bottom:15px;">Net Promoter Score Provisoire (0-100)</label>
                <input type="number" name="nps_score" class="score-input" min="0" max="100" value="70">
                <p style="font-size: 0.8rem; color: #666; margin-top: 10px;">Probabilité que vous recommandiez cet établissement sur une base de 100.</p>
            </div>

            <button type="submit" class="btn-final">TRANSMETTRE AU COMITÉ DE DIRECTION</button>
        </form>
    </div>

    <script>
        function selectPrio(el, val) {
            document.querySelectorAll('.priority-option').forEach(opt => opt.classList.remove('selected-prio'));
            el.classList.add('selected-prio');
            document.getElementById('urgence-input').value = val;
        }
    </script>
    <%- include('partials/forfy') %>
</body>
</html>
