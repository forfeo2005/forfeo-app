<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Plan d'Amélioration | Forfeo Lab</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .survey-container { max-width: 800px; margin: 50px auto; padding: 40px; background: white; border-radius: 15px; border: 2px solid #000; }
        .priority-select { display: flex; gap: 10px; margin-top: 10px; }
        .priority-btn { flex:1; padding:10px; border:1px solid #ddd; background:#fff; cursor:pointer; }
        .priority-btn.active { background:#000; color:#fff; border-color:#000; }
        .btn-final { background: #000; color: white; padding: 20px; border: none; border-radius: 5px; cursor: pointer; font-size: 1.1rem; font-weight: bold; width: 100%; margin-top: 40px; }
    </style>
</head>
<body style="background:#fff; font-family: sans-serif;">
    <div class="survey-container">
        <h1>🚀 Recommandations d'Excellence</h1>
        <p>Proposez des solutions concrètes pour impacter positivement l'excellence de l'entreprise.</p>

        <form action="/submit-survey" method="POST">
            <input type="hidden" name="type" value="satisfaction">

            <div style="margin-bottom:30px;">
                <label style="font-weight:bold;">Quelle est votre recommandation prioritaire ?</label>
                <textarea name="recommandation_principale" style="width:100%; height:100px; margin-top:10px;" placeholder="Action concrète à mettre en place immédiatement..."></textarea>
            </div>

            <div style="margin-bottom:30px;">
                <label style="font-weight:bold;">Niveau d'urgence de l'amélioration :</label>
                <div class="priority-select">
                    <button type="button" class="priority-btn" onclick="setPriority(this, 'Faible')">Faible</button>
                    <button type="button" class="priority-btn" onclick="setPriority(this, 'Moyenne')">Moyenne</button>
                    <button type="button" class="priority-btn" onclick="setPriority(this, 'Urgente')">Urgente 🔥</button>
                </div>
                <input type="hidden" name="urgence" id="urgence-input" value="Moyenne">
            </div>

            <div style="margin-bottom:30px;">
                <label style="font-weight:bold;">Note globale de satisfaction que vous donneriez à l'entreprise :</label>
                <input type="number" name="note_globale" min="0" max="100" style="width:100%; padding:10px; font-size:1.5rem; text-align:center;" placeholder="/100">
            </div>

            <button type="submit" class="btn-final">TRANSMETTRE AU COMITÉ DE DIRECTION</button>
        </form>
    </div>

    <script>
        function setPriority(btn, val) {
            document.querySelectorAll('.priority-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            document.getElementById('urgence-input').value = val;
        }
    </script>

    <%- include('partials/forfy') %>
</body>
</html>
