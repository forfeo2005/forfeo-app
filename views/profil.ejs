<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5" style="max-width: 600px;">
        
        <% if (typeof message !== 'undefined' && message === 'updated') { %>
            <div class="alert alert-success text-center fw-bold shadow-sm mb-4">
                <i class="bi bi-check-circle-fill me-2"></i>Profil mis à jour avec succès !
            </div>
        <% } %>

        <div class="glass-card p-5">
            
            <div class="text-center mb-4">
                <h3 class="fw-bold mb-3 text-primary">Mon Profil</h3>
                
                <% if(user.logo_data) { %>
                    <img src="<%= user.logo_data %>" style="width:100px; height:100px; border-radius:50%; object-fit:cover; border:3px solid #0061ff; margin-bottom:15px; background-color: white;" alt="Logo Entreprise">
                <% } else { %>
                    <div class="d-flex flex-column align-items-center mb-3">
                        <img src="/images/logo-forfeo.png" height="50" alt="Logo Défaut">
                        <span class="brand-lab-neon" style="color:#0061ff; font-weight:800; font-size: 14px;">LAB</span>
                    </div>
                <% } %>
            </div>

            <form action="/profil/update" method="POST">
                <div class="row g-3 mb-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-muted">Nom complet</label>
                        <input type="text" name="nom" value="<%= user.nom %>" class="form-control rounded-pill px-3" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-muted">Adresse Email</label>
                        <input type="email" name="email" value="<%= user.email %>" class="form-control rounded-pill px-3" required>
                    </div>
                </div>
                
                <div class="mb-3">
                    <label class="form-label fw-bold small text-muted">Téléphone</label>
                    <input type="text" name="telephone" value="<%= user.telephone || '' %>" class="form-control rounded-pill px-3" placeholder="(555) 555-5555">
                </div>
                
                <div class="mb-3">
                    <label class="form-label fw-bold small text-muted">Adresse postale</label>
                    <textarea name="adresse" class="form-control rounded-4 px-3 py-2" rows="2" placeholder="123 Rue Exemple, Ville..."><%= user.adresse || '' %></textarea>
                </div>
                
                <div class="mb-4">
                    <label class="form-label fw-bold small text-muted">Nouveau mot de passe (Laisser vide si inchangé)</label>
                    <input type="password" name="new_password" class="form-control rounded-pill px-3" placeholder="******">
                </div>
                
                <button class="btn btn-primary w-100 rounded-pill py-2 fw-bold shadow-sm mb-4">
                    <i class="bi bi-save me-2"></i>Enregistrer les modifications
                </button>
            </form>

            <hr class="border-secondary opacity-10 my-4">

            <div class="text-center d-flex flex-column gap-3">
                <% if (typeof userRole !== 'undefined' && userRole) { %>
                    <a href="/<%= userRole %>/dashboard" class="btn btn-outline-dark rounded-pill fw-bold">
                        <i class="bi bi-arrow-left-circle me-2"></i>Retour à mon Espace
                    </a>
                <% } else { %>
                    <a href="/" class="btn btn-outline-dark rounded-pill fw-bold">
                        <i class="bi bi-house-door-fill me-2"></i>Retour Accueil
                    </a>
                <% } %>

                <form action="/profil/delete" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer votre compte ? Cette action est irréversible.');">
                    <button class="btn btn-link text-danger text-decoration-none small mt-2">
                        <i class="bi bi-trash me-1"></i>Supprimer mon compte définitivement
                    </button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%- include('partials/forfy') %>
</body>
</html>
