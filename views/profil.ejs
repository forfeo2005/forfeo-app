<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Profil | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5" style="max-width: 600px;">
        <div class="glass-card p-5">
            
            <div class="text-center mb-4">
                <h3 class="fw-bold mb-3 text-primary">Mon Profil</h3>
                <div class="d-flex flex-column align-items-center">
                    <img src="/images/logo-forfeo.png" height="40" alt="Logo">
                    <span class="brand-lab-neon" style="color: #0061ff; font-weight: 900; letter-spacing: 4px; font-size: 12px; margin-top: -3px;">LAB</span>
                </div>
            </div>

            <form action="/profil/update" method="POST">
                <div class="row g-3 mb-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-muted">Nom complet</label>
                        <input type="text" name="nom" value="<%= user.nom %>" class="form-control rounded-pill px-3" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-muted">Email</label>
                        <input type="email" name="email" value="<%= user.email %>" class="form-control rounded-pill px-3" required>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold small text-muted">Téléphone</label>
                    <input type="text" name="telephone" value="<%= user.telephone || '' %>" class="form-control rounded-pill px-3" placeholder="(555) 555-5555">
                </div>

                <div class="mb-3">
                    <label class="form-label fw-bold small text-muted">Adresse</label>
                    <textarea name="adresse" class="form-control rounded-4 px-3 py-2" rows="2" placeholder="Votre adresse complète"><%= user.adresse || '' %></textarea>
                </div>

                <div class="mb-4">
                    <label class="form-label fw-bold small text-muted">Nouveau mot de passe (Optionnel)</label>
                    <input type="password" name="new_password" class="form-control rounded-pill px-3" placeholder="Laisser vide pour conserver l'actuel">
                </div>

                <button class="btn btn-primary w-100 rounded-pill py-2 fw-bold shadow-sm mb-4">
                    <i class="bi bi-save me-2"></i>Mettre à jour
                </button>
            </form>

            <hr class="border-secondary opacity-10 my-4">

            <div class="text-center">
                <form action="/profil/delete" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer votre compte ? Cette action est irréversible.');">
                    <button class="btn btn-outline-danger btn-sm rounded-pill px-4 mb-3">
                        <i class="bi bi-trash me-1"></i> Supprimer mon compte
                    </button>
                </form>
                
                <a href="/" class="text-decoration-none text-muted small fw-bold">
                    <i class="bi bi-arrow-left me-1"></i> Retour à l'accueil
                </a>
            </div>

        </div>
    </div>
    <%- include('partials/forfy') %>
</body>
</html>
