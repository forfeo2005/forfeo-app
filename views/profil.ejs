<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <nav class="navbar bg-white border-bottom px-4 py-3">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <button onclick="window.history.back()" class="btn btn-outline-secondary btn-sm rounded-pill">Retour</button>
    </nav>

    <div class="container py-5" style="max-width: 600px;">
        <div class="card border-0 shadow-sm p-4 rounded-4 mb-4">
            <h4 class="fw-bold mb-4">Gérer mon compte</h4>
            
            <% if (message) { %> <div class="alert alert-success"><%= message %></div> <% } %>

            <form action="/update-profil" method="POST">
                <div class="mb-3">
                    <label class="form-label">Nom complet</label>
                    <input type="text" name="nom" class="form-control" value="<%= user.nom %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Mot de passe (optionnel)</label>
                    <input type="password" name="newPassword" class="form-control" placeholder="Laisser vide pour ne pas changer">
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Sauvegarder</button>
            </form>
        </div>

        <div class="card border-danger bg-white p-4 rounded-4 text-center shadow-sm">
            <p class="text-danger small fw-bold">ZONE CRITIQUE</p>
            <form action="/admin/delete-user" method="POST" onsubmit="return confirm('Supprimer définitivement ?');">
                <input type="hidden" name="id_a_supprimer" value="<%= user.id %>">
                <button type="submit" class="btn btn-outline-danger btn-sm">Supprimer mon compte</button>
            </form>
        </div>
    </div>
</body>
</html>
