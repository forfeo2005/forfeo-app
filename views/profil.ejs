<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body class="bg-light d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="ms-auto d-flex align-items-center gap-3">
                <a href="/" class="nav-link fw-bold text-dark small"><i class="bi bi-house-door"></i> Accueil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-3 fw-bold">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1" style="max-width: 600px;">
        <div class="card p-4 border-0 shadow rounded-4 mb-4">
            <h4 class="fw-bold mb-4 text-center">Paramètres du compte</h4>
            <% if (message) { %><div class="alert alert-success small py-2"><%= message %></div><% } %>
            <form action="/update-profil" method="POST">
                <div class="mb-3">
                    <label class="small fw-bold text-muted">NOM COMPLET</label>
                    <input type="text" name="nom" class="form-control" value="<%= user.nom %>" required>
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Sauvegarder</button>
            </form>
        </div>
        <div class="text-center">
            <form action="/admin/delete-user" method="POST" onsubmit="return confirm('Supprimer ?');">
                <input type="hidden" name="id_a_supprimer" value="<%= user.id %>">
                <button type="submit" class="btn btn-link text-danger small p-0 text-decoration-none">Supprimer mon compte</button>
            </form>
        </div>
    </div>

    <footer class="bg-dark text-white py-5 mt-auto">
        <div class="container text-center">
            <p class="small text-white-50">© 2025 FORFEO LAB | Conformité Loi 25 Québec.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
