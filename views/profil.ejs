<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        .card-profile { background: white; border-radius: 20px; padding: 40px; border: none; }
        .form-control { border-radius: 10px; padding: 12px; border: 1px solid #e2e8f0; }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <button onclick="window.history.back()" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Retour</button>
    </nav>

    <div class="container py-5" style="max-width: 600px;">
        <div class="card-profile shadow-sm mb-4">
            <div class="text-center mb-4">
                <div class="bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 80px; height: 80px;">
                    <i class="bi bi-person-fill fs-1"></i>
                </div>
                <h3 class="fw-bold mb-1"><%= user.nom %></h3>
                <span class="badge bg-light text-primary rounded-pill px-3 py-2 border">
                    <%= user.role.toUpperCase() %> | <%= user.forfait %>
                </span>
            </div>

            <% if (message) { %>
                <div class="alert alert-success rounded-3 text-center small mb-4"><%= message %></div>
            <% } %>

            <form action="/update-profil" method="POST">
                <div class="mb-3">
                    <label class="form-label small fw-bold text-muted">NOM COMPLET</label>
                    <input type="text" name="nom" class="form-control" value="<%= user.nom %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label small fw-bold text-muted">COURRIEL</label>
                    <input type="email" class="form-control bg-light" value="<%= user.email %>" disabled>
                </div>
                <hr class="my-4">
                <div class="mb-4">
                    <label class="form-label small fw-bold text-muted">NOUVEAU MOT DE PASSE</label>
                    <input type="password" name="newPassword" class="form-control" placeholder="Laissez vide pour conserver l'actuel">
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill py-3 fw-bold shadow">Enregistrer</button>
            </form>
        </div>

        <div class="card border-danger bg-light rounded-4 p-4 text-center">
            <h6 class="text-danger fw-bold small"><i class="bi bi-exclamation-triangle-fill"></i> ZONE DE DANGER</h6>
            <form action="/admin/delete-user" method="POST" onsubmit="return confirm('Supprimer définitivement votre compte ?');">
                <input type="hidden" name="id_a_supprimer" value="<%= user.id %>">
                <button type="submit" class="btn btn-link text-danger p-0 small">Supprimer mon compte</button>
            </form>
        </div>
    </div>
</body>
</html>
