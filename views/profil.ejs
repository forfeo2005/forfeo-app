<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5" style="max-width: 600px;">
        <div class="glass-card p-5">
            
            <div class="text-center mb-4">
                <h3 class="fw-bold mb-3 text-primary">Mon Profil</h3>
                
                <% if(user.logo_data) { %>
                    <img src="<%= user.logo_data %>" style="width:80px; height:80px; border-radius:50%; object-fit:cover; border:3px solid #0061ff; margin-bottom:15px;">
                <% } else { %>
                    <div class="d-flex flex-column align-items-center">
                        <img src="/images/logo-forfeo.png" height="40" alt="Logo">
                        <span class="brand-lab-neon">LAB</span>
                    </div>
                <% } %>
            </div>

            <form action="/profil/update" method="POST">
                <div class="row g-3 mb-3">
                    <div class="col-md-6"><label class="form-label fw-bold small text-muted">Nom</label><input type="text" name="nom" value="<%= user.nom %>" class="form-control rounded-pill px-3" required></div>
                    <div class="col-md-6"><label class="form-label fw-bold small text-muted">Email</label><input type="email" name="email" value="<%= user.email %>" class="form-control rounded-pill px-3" required></div>
                </div>
                <div class="mb-3"><label class="form-label fw-bold small text-muted">Téléphone</label><input type="text" name="telephone" value="<%= user.telephone || '' %>" class="form-control rounded-pill px-3"></div>
                <div class="mb-3"><label class="form-label fw-bold small text-muted">Adresse</label><textarea name="adresse" class="form-control rounded-4 px-3 py-2" rows="2"><%= user.adresse || '' %></textarea></div>
                <div class="mb-4"><label class="form-label fw-bold small text-muted">Nouveau MDP (Optionnel)</label><input type="password" name="new_password" class="form-control rounded-pill px-3"></div>
                <button class="btn btn-primary w-100 rounded-pill py-2 fw-bold shadow-sm mb-4">Mettre à jour</button>
            </form>

            <hr class="border-secondary opacity-10 my-4">

            <div class="text-center d-flex flex-column gap-3">
                <% if (userRole) { %>
                    <a href="/<%= userRole %>/dashboard" class="btn btn-outline-dark rounded-pill fw-bold">
                        <i class="bi bi-arrow-left-circle me-2"></i>Retour à mon Espace
                    </a>
                <% } else { %>
                    <a href="/" class="btn btn-outline-dark rounded-pill fw-bold">Retour Accueil</a>
                <% } %>

                <form action="/profil/delete" method="POST" onsubmit="return confirm('Irréversible. Continuer ?');">
                    <button class="btn btn-link text-danger text-decoration-none small">Supprimer mon compte</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
