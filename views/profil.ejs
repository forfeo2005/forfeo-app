<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5" style="max-width: 600px;">
        <div class="glass-card">
            <h3 class="fw-bold mb-4">Mon Profil</h3>
            <div class="text-center mb-4"><img src="/images/logo-forfeo.png" height="40"><span class="brand-lab-neon">LAB</span></div>
            <form action="/profil/update" method="POST">
                <div class="row g-2">
                    <div class="col-md-6 mb-3"><label class="fw-bold small">Nom</label><input type="text" name="nom" value="<%= user.nom %>" class="form-control" required></div>
                    <div class="col-md-6 mb-3"><label class="fw-bold small">Email</label><input type="email" name="email" value="<%= user.email %>" class="form-control" required></div>
                </div>
                <div class="mb-3"><label class="fw-bold small">Téléphone</label><input type="text" name="telephone" value="<%= user.telephone || '' %>" class="form-control"></div>
                <div class="mb-3"><label class="fw-bold small">Adresse</label><textarea name="adresse" class="form-control" rows="2"><%= user.adresse || '' %></textarea></div>
                <div class="mb-3"><label class="fw-bold small">Nouveau MDP (Optionnel)</label><input type="password" name="new_password" class="form-control mb-4"></div>
                <button class="btn btn-primary w-100 rounded-pill mb-3">Mettre à jour</button>
            </form>
            <hr class="my-4"><div class="text-center"><a href="/" class="d-block mt-3 text-muted small">Retour à l'accueil</a></div>
        </div>
    </div>
    <%- include('partials/forfy') %>
</body>
</html>
