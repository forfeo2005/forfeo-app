<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5" style="max-width: 600px;">
        <div class="glass-card">
            <h3 class="fw-bold mb-4">Mon Profil</h3>
            <% if(message) { %><div class="alert alert-success"><%= message %></div><% } %>
            
            <form action="/profil/update" method="POST">
                <div class="mb-3">
                    <label class="fw-bold small text-muted">Nom Complet</label>
                    <input type="text" name="nom" value="<%= user.nom %>" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="fw-bold small text-muted">Email</label>
                    <input type="email" name="email" value="<%= user.email %>" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="fw-bold small text-muted">Nouveau Mot de passe (laisser vide si inchangé)</label>
                    <input type="password" name="new_password" class="form-control">
                </div>
                <button class="btn btn-primary w-100 rounded-pill mb-4">Mettre à jour</button>
            </form>

            <hr>
            <div class="text-center">
                <form action="/profil/delete" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer votre compte ? Cette action est irréversible.');">
                    <button class="btn btn-outline-danger btn-sm rounded-pill">Supprimer mon compte</button>
                </form>
                <a href="/" class="d-block mt-3 text-muted small">Retour au tableau de bord</a>
            </div>
        </div>
    </div>
</body>
</html>
