<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        .profile-card { background: white; border-radius: 20px; padding: 30px; border: none; }
        .form-control { border-radius: 10px; padding: 12px; border: 1px solid #e2e8f0; }
        @media (max-width: 768px) { .container { padding: 15px; } }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-3 py-2 sticky-top">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <button onclick="window.history.back()" class="btn btn-outline-secondary btn-sm rounded-pill">Retour</button>
    </nav>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="profile-card shadow-sm">
                    <h2 class="fw-bold mb-4">Paramètres du profil ⚙️</h2>
                    
                    <% if (typeof message !== 'undefined') { %>
                        <div class="alert alert-success rounded-3"><%= message %></div>
                    <% } %>

                    <form action="/update-profil" method="POST">
                        <div class="mb-3">
                            <label class="form-label text-muted small fw-bold">NOM COMPLET</label>
                            <input type="text" name="nom" class="form-control" value="<%= user.nom %>" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label text-muted small fw-bold">EMAIL (Non modifiable)</label>
                            <input type="email" class="form-control bg-light" value="<%= user.email %>" disabled>
                        </div>
                        <hr class="my-4">
                        <h6 class="fw-bold mb-3 text-primary">Changer le mot de passe</h6>
                        <div class="mb-3">
                            <label class="form-label text-muted small fw-bold">NOUVEAU MOT DE PASSE</label>
                            <input type="password" name="newPassword" class="form-control" placeholder="Laissez vide pour ne pas changer">
                        </div>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill py-3 fw-bold mt-3 shadow">
                            Enregistrer les modifications
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
