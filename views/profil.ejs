<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><title>Mon Profil | FORFEO LAB</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"></head>
<body class="bg-light">
    <nav class="navbar bg-white border-bottom py-3">
        <div class="container">
            <a href="/" class="btn btn-outline-secondary rounded-pill btn-sm shadow-sm"><i class="bi bi-house"></i> Accueil</a>
            <span class="navbar-brand fw-bold text-primary mx-auto">PARAMÈTRES</span>
            <a href="/logout" class="btn btn-danger btn-sm rounded-pill shadow-sm">Déconnexion</a>
        </div>
    </nav>
    <div class="container py-5" style="max-width: 500px;">
        <div class="card border-0 shadow-sm rounded-4 p-5 text-center">
            <div class="mb-4"><i class="bi bi-person-circle text-primary" style="font-size: 4rem;"></i><h3 class="fw-bold mt-2"><%= user.nom %></h3><p class="text-muted"><%= user.email %></p></div>
            <% if (message) { %><div class="alert alert-success rounded-pill"><%= message %></div><% } %>
            <hr class="my-4">
            <form action="/profil/update-password" method="POST" class="text-start">
                <label class="small fw-bold text-muted mb-2">NOUVEAU MOT DE PASSE</label>
                <input type="password" name="new_password" class="form-control rounded-pill mb-4 shadow-sm" required>
                <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold shadow">Mettre à jour</button>
            </form>
        </div>
    </div>
</body>
</html>
