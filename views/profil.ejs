<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex flex-column min-vh-100 bg-light">
    <div class="container py-5 flex-grow-1" style="max-width: 600px;">
        <div class="card p-4 border-0 shadow rounded-4 mb-4">
            <h4 class="fw-bold mb-4 text-center text-primary">Paramètres du compte</h4>
            <% if (message) { %><div class="alert alert-success small py-2"><%= message %></div><% } %>
            <form action="/update-profil" method="POST">
                <div class="mb-3">
                    <label class="small fw-bold text-muted">NOM COMPLET</label>
                    <input type="text" name="nom" class="form-control" value="<%= user.nom %>">
                </div>
                <div class="mb-4">
                    <label class="small fw-bold text-muted">NOUVEAU MOT DE PASSE</label>
                    <input type="password" name="newPassword" class="form-control" placeholder="Laisser vide si inchangé">
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Sauvegarder les modifications</button>
            </form>
            <hr class="my-4">
            <div class="text-center">
                <a href="/logout" class="btn btn-outline-dark btn-sm rounded-pill px-4">Déconnexion</a>
            </div>
        </div>

        <form action="/admin/delete-user" method="POST" class="text-center" onsubmit="return confirm('Attention : suppression irréversible. Confirmer ?');">
            <input type="hidden" name="id_a_supprimer" value="<%= user.id %>">
            <button type="submit" class="btn btn-link text-danger small text-decoration-none">Supprimer mon compte définitivement</button>
        </form>
    </div>

    <footer class="bg-dark text-white py-5 mt-auto">
        <div class="container text-center">
            <div class="row align-items-center mb-4">
                <div class="col-md-6 text-md-start mb-3 mb-md-0">
                    <h5 class="fw-bold text-primary mb-1">FORFEO LAB</h5>
                    <p class="small text-white-50 mb-0">&copy; 2025 - Tous droits réservés au Québec.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="d-flex justify-content-center justify-content-md-end gap-3 small">
                        <a href="/politique-confidentialite" class="text-white-50 text-decoration-none hover-link">Confidentialité (Loi 25)</a>
                        <span class="text-white-25">|</span>
                        <a href="/conditions-utilisation" class="text-white-50 text-decoration-none hover-link">Conditions d'utilisation</a>
                    </div>
                </div>
            </div>
            <p class="x-small text-white-50 border-top border-secondary pt-3" style="font-size: 0.75rem; opacity: 0.6;">
                <strong>Avis Juridique :</strong> FORFEO LAB est une plateforme de mise en relation technologique. 
                Les Ambassadeurs (Testeurs) réalisent des mandats à titre de <strong>prestataires de services indépendants</strong> 
                et ne sont pas des employés de la plateforme ou des entreprises clientes.
            </p>
        </div>
    </footer>
    <style>.hover-link:hover { color: #007bff !important; text-decoration: underline !important; }</style>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
