<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mon Profil | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5" style="max-width: 600px;">
        <div class="card p-4 border-0 shadow rounded-4 mb-4">
            <h4 class="fw-bold mb-4">Gérer mon compte</h4>
            <% if (message) { %><div class="alert alert-success small"><%= message %></div><% } %>
            <form action="/update-profil" method="POST">
                <div class="mb-3"><label class="form-label">Nom complet</label>
                <input type="text" name="nom" class="form-control" value="<%= user.nom %>" required></div>
                <div class="mb-4"><label class="form-label">Nouveau mot de passe</label>
                <input type="password" name="newPassword" class="form-control" placeholder="Laissez vide pour ne pas changer"></div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Sauvegarder</button>
            </form>
        </div>
        <div class="card border-danger p-4 rounded-4 text-center">
            <form action="/admin/delete-user" method="POST" onsubmit="return confirm('Supprimer définitivement ?');">
                <input type="hidden" name="id_a_supprimer" value="<%= user.id %>">
                <button type="submit" class="btn btn-link text-danger p-0 small">Supprimer mon compte</button>
            </form>
        </div>
    </div>
    </body>
</html>
