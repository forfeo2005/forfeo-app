<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mes Missions | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .mission-card { border: none; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); background: white; margin-bottom: 20px; }
        .badge-status { font-size: 0.75rem; padding: 6px 12px; border-radius: 50px; text-transform: uppercase; font-weight: bold; }
        .badge-reserve { background: #fff4e5; color: #b76e00; }
        .badge-termine { background: #e6fffa; color: #008767; }
        .reward { color: #0d6efd; font-weight: bold; font-size: 1.2rem; }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom p-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/ambassadeur/dashboard">← Retour aux offres</a>
            <span class="navbar-text fw-bold">Mes Audits Réservés</span>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <h2 class="fw-bold mb-4">Suivi de mes missions</h2>

                <% if (missions.length > 0) { %>
                    <% missions.forEach(m => { %>
                        <div class="card mission-card p-4">
                            <div class="d-flex justify-content-between align-items-start mb-3">
                                <div>
                                    <% if(m.statut === 'termine') { %>
                                        <span class="badge badge-status badge-termine">Terminé ✅</span>
                                    <% } else { %>
                                        <span class="badge badge-status badge-reserve">En cours ⏳</span>
                                    <% } %>
                                    <h4 class="fw-bold mt-2"><%= m.titre %></h4>
                                </div>
                                <div class="reward"><%= m.recompense %></div>
                            </div>

                            <p class="text-muted"><%= m.description %></p>
                            <hr>

                            <% if(m.statut !== 'termine') { %>
                                <div class="bg-light p-3 rounded-3">
                                    <h6 class="fw-bold mb-3">Rapport d'audit mystère</h6>
                                    <form action="/envoyer-rapport" method="POST">
                                        <input type="hidden" name="id_mission" value="<%= m.id %>">
                                        <textarea name="rapport" class="form-control border-0 mb-3" rows="4" placeholder="Décrivez ici votre expérience, la qualité du service, la propreté, etc." required></textarea>
                                        <button type="submit" class="btn btn-primary rounded-pill w-100 fw-bold">Envoyer le rapport final</button>
                                    </form>
                                </div>
                            <% } else { %>
                                <div class="alert alert-success border-0 rounded-3">
                                    <strong>Rapport soumis :</strong><br>
                                    <small class="text-dark"><%= m.rapport_final %></small>
                                </div>
                            <% } %>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="text-center py-5">
                        <p class="text-muted">Vous n'avez aucune mission réservée pour le moment.</p>
                        <a href="/ambassadeur/dashboard" class="btn btn-outline-primary rounded-pill">Explorer les missions</a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
