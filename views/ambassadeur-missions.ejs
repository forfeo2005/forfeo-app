<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mes Missions | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        .mission-item { background: white; border-radius: 15px; padding: 20px; border-left: 5px solid #007bff; }
        .badge-status { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px; }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <a class="navbar-brand fw-bold text-primary" href="/ambassadeur/dashboard">FORFEO LAB</a>
            <div class="d-flex align-items-center gap-2">
                <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill px-3">
                    <i class="bi bi-person-circle me-1"></i> Profil
                </a>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">
                    <i class="bi bi-box-arrow-right"></i>
                </a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="mb-4">
            <a href="/ambassadeur/dashboard" class="btn btn-link text-decoration-none p-0 mb-2">
                <i class="bi bi-arrow-left"></i> Retour aux offres
            </a>
            <h2 class="fw-bold">Mes Audits en cours 📋</h2>
        </div>

        <div class="row g-3">
            <% if (missions.length === 0) { %>
                <div class="text-center py-5">
                    <p class="text-muted">Vous n'avez pas encore réservé de mission.</p>
                </div>
            <% } %>

            <% missions.forEach(m => { %>
                <div class="col-12">
                    <div class="mission-item shadow-sm">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <span class="badge badge-status <%= m.statut === 'approuve' ? 'bg-success' : 'bg-warning text-dark' %> mb-2">
                                    <%= m.statut %>
                                </span>
                                <h5 class="fw-bold mb-1"><%= m.titre %></h5>
                                <p class="text-muted small mb-0"><%= m.recompense %></p>
                            </div>
                            <% if (m.statut === 'reserve') { %>
                                <button class="btn btn-primary rounded-pill btn-sm px-4" data-bs-toggle="modal" data-bs-target="#modalRapport<%= m.id %>">
                                    Soumettre
                                </button>
                            <% } %>
                        </div>
                    </div>
                </div>

                <div class="modal fade" id="modalRapport<%= m.id %>" tabindex="-1">
                    <div class="modal-dialog modal-dialog-centered">
                        <div class="modal-content p-4 rounded-4 border-0">
                            <h5 class="fw-bold mb-3">Rapport d'audit : <%= m.titre %></h5>
                            <form action="/envoyer-rapport" method="POST">
                                <input type="hidden" name="id_mission" value="<%= m.id %>">
                                <div class="mb-3">
                                    <label class="form-label small fw-bold">VOTRE FEEDBACK</label>
                                    <textarea name="feedback" class="form-control" rows="4" placeholder="Décrivez votre expérience..." required></textarea>
                                </div>
                                <div class="mb-4">
                                    <label class="form-label small fw-bold">PHOTO DE PREUVE (URL)</label>
                                    <input type="url" name="photo_url" class="form-control" placeholder="https://..." required>
                                </div>
                                <button type="submit" class="btn btn-success w-100 rounded-pill py-2 fw-bold shadow">
                                    Finaliser la mission
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
