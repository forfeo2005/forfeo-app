<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre Avis Compte</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; }
        .survey-card { max-width: 600px; margin: 50px auto; background: white; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); padding: 40px; border: 1px solid #f0f0f0; }
        .rating-stars { display: flex; flex-direction: row-reverse; justify-content: center; gap: 10px; }
        .rating-stars input { display: none; }
        .rating-stars label { font-size: 30px; color: #ddd; cursor: pointer; transition: color 0.2s; }
        .rating-stars input:checked ~ label, .rating-stars label:hover, .rating-stars label:hover ~ label { color: #ffc107; }
        .logo-placeholder { width: 80px; height: 80px; background: #f0f7ff; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px; font-size: 30px; color: #0061ff; }
    </style>
</head>
<body>
    <div class="container">
        <div class="survey-card">
            <div class="text-center">
                <div class="logo-placeholder"><i class="bi bi-shop"></i></div>
                <h3 class="fw-bold mb-2"><%= entreprise.nom %></h3>
                <p class="text-muted mb-4">Merci de prendre un moment pour partager votre expérience.</p>
            </div>

            <form action="/sondage-client/submit" method="POST">
                <input type="hidden" name="entreprise_id" value="<%= entreprise.id %>">
                <input type="hidden" name="entreprise_nom" value="<%= entreprise.nom %>">
                <input type="hidden" name="type_activite" value="<%= type %>">

                <% questions.forEach((q, index) => { %>
                    <div class="mb-4">
                        <label class="form-label fw-bold mb-2"><%= q.text %></label>
                        
                        <% if(q.type === 'stars') { %>
                            <div class="rating-stars">
                                <input type="radio" name="<%= q.id %>" id="<%= q.id %>_5" value="5"><label for="<%= q.id %>_5">★</label>
                                <input type="radio" name="<%= q.id %>" id="<%= q.id %>_4" value="4"><label for="<%= q.id %>_4">★</label>
                                <input type="radio" name="<%= q.id %>" id="<%= q.id %>_3" value="3"><label for="<%= q.id %>_3">★</label>
                                <input type="radio" name="<%= q.id %>" id="<%= q.id %>_2" value="2"><label for="<%= q.id %>_2">★</label>
                                <input type="radio" name="<%= q.id %>" id="<%= q.id %>_1" value="1"><label for="<%= q.id %>_1">★</label>
                            </div>
                        <% } else if(q.type === 'yesno') { %>
                            <div class="d-flex gap-3 justify-content-center">
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="<%= q.id %>" value="Oui" id="<%= q.id %>_oui">
                                    <label class="form-check-label" for="<%= q.id %>_oui">Oui</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="<%= q.id %>" value="Non" id="<%= q.id %>_non">
                                    <label class="form-check-label" for="<%= q.id %>_non">Non</label>
                                </div>
                            </div>
                        <% } else { %>
                            <textarea class="form-control" name="<%= q.id %>" rows="2" placeholder="Votre réponse..."></textarea>
                        <% } %>
                    </div>
                <% }) %>

                <div class="d-grid mt-5">
                    <button type="submit" class="btn btn-primary btn-lg rounded-pill fw-bold shadow-sm">Envoyer mon avis</button>
                </div>
                
                <div class="text-center mt-4">
                    <small class="text-muted" style="font-size: 10px;">Propulsé par <strong>Forfeo Lab</strong></small>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
