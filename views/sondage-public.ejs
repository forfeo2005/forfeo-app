<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre Avis - <%= entreprise.nom %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; min-height: 100vh; }
        .survey-card { max-width: 600px; margin: 40px auto; background: white; border-radius: 20px; box-shadow: 0 10px 40px rgba(0,0,0,0.05); overflow: hidden; border: 1px solid #f0f0f0; }
        .survey-header { background: linear-gradient(135deg, #0061ff, #00c6ff); padding: 40px 20px; text-align: center; color: white; }
        .logo-circle { width: 100px; height: 100px; background: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 32px; font-weight: 800; color: #0061ff; margin: 0 auto 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.2); overflow: hidden; }
        .logo-img { width: 100%; height: 100%; object-fit: cover; }
        .rating-stars { display: flex; flex-direction: row-reverse; justify-content: center; gap: 8px; }
        .rating-stars input { display: none; }
        .rating-stars label { font-size: 40px; color: #e2e8f0; cursor: pointer; transition: 0.2s; }
        .rating-stars input:checked ~ label, .rating-stars label:hover, .rating-stars label:hover ~ label { color: #fbbf24; }
    </style>
</head>
<body>
    <div class="container px-3">
        <div class="survey-card">
            <div class="survey-header">
                <div class="logo-circle">
                    <% if(entreprise.logo_data) { %>
                        <img src="<%= entreprise.logo_data %>" class="logo-img" alt="Logo">
                    <% } else { %>
                        <%= entreprise.nom.substring(0, 2).toUpperCase() %>
                    <% } %>
                </div>
                <h3 class="fw-bold mb-1"><%= entreprise.nom %></h3>
                <p class="mb-0 opacity-75 small text-uppercase fw-bold"><%= type %></p>
            </div>

            <div class="p-4 p-md-5">
                <form action="/sondage-client/submit" method="POST">
                    <input type="hidden" name="entreprise_id" value="<%= entreprise.id %>">
                    <input type="hidden" name="entreprise_nom" value="<%= entreprise.nom %>">
                    <input type="hidden" name="type_activite" value="<%= type %>">

                    <% questions.forEach((q) => { %>
                        <div class="mb-5 text-center">
                            <label class="form-label fw-bold mb-3 d-block text-dark h5"><%= q.text %></label>
                            
                            <% if(q.type === 'stars') { %>
                                <div class="rating-stars">
                                    <input type="radio" name="<%= q.id %>" id="<%= q.id %>_5" value="5" required><label for="<%= q.id %>_5">★</label>
                                    <input type="radio" name="<%= q.id %>" id="<%= q.id %>_4" value="4"><label for="<%= q.id %>_4">★</label>
                                    <input type="radio" name="<%= q.id %>" id="<%= q.id %>_3" value="3"><label for="<%= q.id %>_3">★</label>
                                    <input type="radio" name="<%= q.id %>" id="<%= q.id %>_2" value="2"><label for="<%= q.id %>_2">★</label>
                                    <input type="radio" name="<%= q.id %>" id="<%= q.id %>_1" value="1"><label for="<%= q.id %>_1">★</label>
                                </div>
                            <% } else if(q.type === 'yesno') { %>
                                <div class="d-flex justify-content-center gap-3">
                                    <input type="radio" class="btn-check" name="<%= q.id %>" id="<%= q.id %>_oui" value="Oui" required>
                                    <label class="btn btn-lg btn-outline-success px-4 rounded-pill" for="<%= q.id %>_oui">Oui</label>
                                    <input type="radio" class="btn-check" name="<%= q.id %>" id="<%= q.id %>_non" value="Non">
                                    <label class="btn btn-lg btn-outline-danger px-4 rounded-pill" for="<%= q.id %>_non">Non</label>
                                </div>
                            <% } else { %>
                                <textarea class="form-control bg-light border-0 p-3 rounded-3" name="<%= q.id %>" rows="3" placeholder="Écrivez ici..."></textarea>
                            <% } %>
                        </div>
                    <% }) %>

                    <div class="d-grid">
                        <button class="btn btn-primary btn-lg rounded-pill py-3 fw-bold shadow-lg">Envoyer mon avis</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="text-center pb-5"><small class="text-muted">Propulsé par <strong>Forfeo Lab</strong></small></div>
    </div>
</body>
</html>
