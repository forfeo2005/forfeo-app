<div class="forfy-wrapper">
    <div class="forfy-chat-box" id="forfyChat">
        <div class="chat-header">
            <strong>Forfy AI</strong>
            <span class="status-dot"></span>
        </div>
        <div class="chat-content" id="chatContent">
            Bienvenue chez Forfeo Canada ! Comment puis-je vous aider aujourd'hui ? 🐾
        </div>
        <div class="chat-options" id="chatOptions">
            <button onclick="handleForfy('ambassadeur')">Devenir Ambassadeur 🤝</button>
            <button onclick="handleForfy('tarifs')">Voir les Tarifs CAD 💰</button>
            <button onclick="handleForfy('audit')">Ma mission d'audit 📋</button>
        </div>
    </div>

    <div class="forfy-trigger" onclick="toggleForfy()">
        <video class="forfy-video-circle" src="/videos/GifForfy.MP4" autoplay loop muted playsinline></video>
        <div class="notification-badge">1</div>
    </div>
</div>

<style>
    .forfy-wrapper { position: fixed; bottom: 30px; right: 30px; z-index: 10000; font-family: 'Inter', sans-serif; }
    
    /* Le Cercle Vidéo */
    .forfy-trigger { position: relative; cursor: pointer; transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
    .forfy-trigger:hover { transform: scale(1.1); }
    .forfy-video-circle { width: 90px; height: 90px; border-radius: 50%; border: 3px solid #2563eb; background: #000; object-fit: cover; box-shadow: 0 10px 25px rgba(0,0,0,0.3); }
    
    .notification-badge { position: absolute; top: 0; right: 5px; background: #ff4757; color: white; font-size: 10px; padding: 4px 8px; border-radius: 50%; border: 2px solid #050505; }

    /* Fenêtre de Chat */
    .forfy-chat-box { 
        position: absolute; bottom: 110px; right: 0; width: 280px; 
        background: rgba(255, 255, 255, 0.98); border-radius: 20px; 
        box-shadow: 0 15px 35px rgba(0,0,0,0.2); 
        display: none; flex-direction: column; overflow: hidden;
        animation: slideUp 0.4s ease;
    }
    @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

    .chat-header { background: #2563eb; color: white; padding: 12px 20px; font-size: 14px; display: flex; align-items: center; justify-content: space-between; }
    .status-dot { width: 8px; height: 8px; background: #2ecc71; border-radius: 50%; box-shadow: 0 0 10px #2ecc71; }
    
    .chat-content { padding: 20px; font-size: 13px; color: #1e293b; line-height: 1.5; border-bottom: 1px solid #f1f5f9; }
    
    .chat-options { padding: 10px; display: flex; flex-direction: column; gap: 8px; }
    .chat-options button { 
        border: 1px solid #e2e8f0; background: white; padding: 10px; 
        border-radius: 10px; font-size: 12px; font-weight: 600; color: #2563eb;
        cursor: pointer; transition: 0.2s; text-align: left;
    }
    .chat-options button:hover { background: #f8fafc; border-color: #2563eb; }
</style>
