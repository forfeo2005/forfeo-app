<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Forfeo Lab | L'Excellence Opérationnelle</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://unpkg.com/scrollreveal"></script>
    <style>
        .hero-wow {
            background: linear-gradient(135deg, #ffffff 0%, #eef2f7 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
        }
        .reveal { opacity: 0; transform: translateY(30px); transition: all 0.8s ease; }
        .reveal.active { opacity: 1; transform: translateY(0); }
        
        .floating-video {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 150px;
            z-index: 1000;
        }
        .chat-bubble {
            position: absolute;
            top: -50px;
            right: 0;
            background: var(--primary);
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(0,82,204,0.3);
            white-space: nowrap;
        }
    </style>
</head>
<body>
    <div class="hero-wow">
        <h1 class="reveal" style="font-size: 4rem; color: var(--dark);">Forfeo <span style="color: var(--primary);">Lab</span></h1>
        <p class="reveal" style="font-size: 1.5rem; color: var(--text-sub);">L'élite de l'audit client au service de votre croissance.</p>
        
        <div class="portal-grid container">
            <div class="card portal-card reveal">
                <h3>Entreprises</h3>
                <p>Pilotez votre réputation avec des données certifiées.</p>
                <a href="/entreprise/dashboard" class="btn-primary">Accès Dashboard</a>
            </div>
            <div class="card portal-card reveal">
                <h3>Ambassadeurs</h3>
                <p>Devenez critique exclusif. Vivez des expériences gratuites en échange de votre expertise.</p>
                <a href="/ambassadeur/inscription" class="btn-primary">Postuler au programme</a>
            </div>
        </div>
    </div>

    <div class="floating-video">
        <div class="chat-bubble" id="forfyText">Besoin d'aide ?</div>
        <video src="/videos/forfy.mp4" autoplay loop muted style="width:100%; border-radius: 50%; border: 3px solid var(--primary);"></video>
    </div>

    <script>
        window.addEventListener('scroll', () => {
            document.querySelectorAll('.reveal').forEach(el => {
                if(el.getBoundingClientRect().top < window.innerHeight) el.classList.add('active');
            });
        });
        window.onload = () => document.querySelectorAll('.reveal').forEach(el => el.classList.add('active'));
    </script>
</body>
</html>
