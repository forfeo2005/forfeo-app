<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Académie | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm">
        <div class="container"><a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a></div>
    </nav>
    <div class="container py-5">
        <h2 class="fw-bold mb-4">Académie de Formation Professionnelle</h2>
        <div class="row g-4">
            <% modules.forEach(m => { %>
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm rounded-4 p-4">
                        <div class="mb-3 text-primary"><i class="bi bi-mortarboard-fill fs-2"></i></div>
                        <h5 class="fw-bold"><%= m.titre %></h5>
                        <p class="small text-muted mb-4"><%= m.description %></p>
                        <a href="/formations/module/<%= m.id %>" class="btn btn-primary rounded-pill w-100 mt-auto fw-bold">Accéder au module</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
