<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Académie | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-light bg-white border-bottom py-3 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="d-flex align-items-center">
                <span class="me-3 fw-bold"><%= userName %></span>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row mb-4">
            <div class="col-12">
                <h2 class="fw-bold"><i class="bi bi-mortarboard-fill text-primary"></i> Académie de Formation</h2>
                <p class="text-muted text-uppercase small">Modules obligatoires et standards d'excellence.</p>
            </div>
        </div>

        <div class="row g-4">
            <% modules.forEach(m => { %>
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm rounded-4 p-4 transition-hover">
                        <div class="mb-3"><span class="badge bg-primary-subtle text-primary rounded-pill px-3">Module Officiel</span></div>
                        <h5 class="fw-bold"><%= m.titre %></h5>
                        <p class="small text-muted mb-4"><%= m.description %></p>
                        <a href="/formations/module/<%= m.id %>" class="btn btn-primary rounded-pill w-100 mt-auto fw-bold shadow-sm">Accéder au cours</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer class="bg-dark text-white py-4 mt-5 text-center">
        <p class="small text-white-50 m-0">FORFEO Academy © 2025 | Standard de formation conforme au Québec.</p>
    </footer>
</body>
</html>
