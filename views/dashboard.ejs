<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <title>Tableau de Bord - FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#">FORFEO PRO</a>
            <div class="d-flex text-white align-items-center">
                <span class="me-3 small">Connecté : <strong><%= user.nom %></strong></span>
                <a href="/login" class="btn btn-sm btn-outline-light rounded-pill">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row mb-4 align-items-end">
            <div class="col-md-8">
                <h1 class="fw-bold text-dark">Tableau de Bord</h1>
                <p class="text-muted mb-0">Bienvenue dans votre espace de gestion.</p>
            </div>
            <div class="col-md-4 text-md-end">
                <span class="badge bg-primary fs-6 px-3 py-2 rounded-pill">Plan : <%= user.plan %></span>
            </div>
        </div>

        <div class="row g-4 mb-5">
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center p-4">
                        <i class="bi bi-trophy text-warning display-4 mb-3"></i>
                        <h5 class="text-muted text-uppercase small fw-bold">Score Qualité</h5>
                        <h2 class="display-5 fw-bold text-dark"><%= user.score %>/10</h2>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center p-4">
                        <i class="bi bi-clipboard-check text-info display-4 mb-3"></i>
                        <h5 class="text-muted text-uppercase small fw-bold">Missions Commandées</h5>
                        <h2 class="display-5 fw-bold text-dark"><%= missions.length %></h2>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100 bg-dark text-white">
                    <div class="card-body text-center p-4 d-flex flex-column justify-content-center">
                        <h5 class="mb-3">Besoin d'un audit ?</h5>
                        <a href="/new-mission?id=<%= user.id %>" class="btn btn-light rounded-pill fw-bold">
                            <i class="bi bi-plus-circle me-1"></i> Nouvelle Mission
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <h4 class="fw-bold mb-3">Historique des Missions</h4>
        <div class="card border-0 shadow-sm">
            <div class="card-body p-0">
                <div class="list-group list-group-flush">
                    
                    <% if (missions.length === 0) { %>
                        <div class="list-group-item p-5 text-center text-muted">
                            <i class="bi bi-inbox fs-1 d-block mb-3"></i>
                            <p>Aucune mission pour le moment.<br>Cliquez sur le bouton noir pour commencer.</p>
                        </div>
                    <% } else { %>
                        
                        <% missions.forEach(function(mission) { 
                            // --- LOGIQUE DES COULEURS (ICI !) ---
                            let badgeClass = 'bg-secondary';
                            let iconClass = 'bi-circle';
                            
                            if (mission.statut === 'En attente') {
                                badgeClass = 'bg-warning text-dark';
                                iconClass = 'bi-hourglass-split';
                            } else if (mission.statut === 'En cours') {
                                badgeClass = 'bg-info text-dark';
                                iconClass = 'bi-play-circle';
                            } else if (mission.statut === 'Terminée') {
                                badgeClass = 'bg-success';
                                iconClass = 'bi-check-circle-fill';
                            } else if (mission.statut === 'Annulée') {
                                badgeClass = 'bg-danger';
                                iconClass = 'bi-x-circle';
                            }
                        %>

                        <div class="list-group-item p-4">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h5 class="fw-bold mb-1"><%= mission.type_mission %></h5>
                                    <p class="text-muted small mb-2">
                                        <i class="bi bi-calendar-event me-1"></i> Prévu le : <%= mission.date_souhaitee %>
                                    </p>
                                    <p class="mb-0 text-muted fst-italic">"<%= mission.details %>"</p>
                                </div>
                                <div class="text-end">
                                    <span class="badge <%= badgeClass %> px-3 py-2 rounded-pill d-flex align-items-center gap-2">
                                        <i class="bi <%= iconClass %>"></i> <%= mission.statut %>
                                    </span>
                                </div>
                            </div>
                        </div>

                        <% }); %>
                    <% } %>

                </div>
            </div>
        </div>

    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
