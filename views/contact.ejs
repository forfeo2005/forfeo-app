<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Support & Contact | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .contact-card {
            border-radius: 20px;
            overflow: hidden;
            transition: transform 0.3s ease;
        }
        /* Effet qui bouge au survol */
        .contact-card:hover {
            transform: translateY(-10px);
        }
        .btn-send {
            transition: all 0.3s ease;
            font-weight: bold;
        }
        .btn-send:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(0,123,255,0.2);
        }
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navContact">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navContact">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="/ambassadeur/details">L'Impact</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="contact-card bg-white shadow-lg p-4 p-md-5">
                    <h2 class="fw-bold text-center mb-4">Contactez le Support</h2>
                    <p class="text-center text-muted mb-5">Une question sur vos missions ou votre abonnement ? Notre équipe vous répond sous 24h.</p>
                    
                    <form action="/envoyer-contact" method="POST">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Votre Nom</label>
                            <input type="text" name="nom" class="form-control rounded-pill px-4" placeholder="Ex: Jean Dupont" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Sujet</label>
                            <select name="sujet" class="form-select rounded-pill px-4">
                                <option value="Question Générale">Question Générale</option>
                                <option value="Problème Technique">Problème Technique</option>
                                <option value="Paiement / Premium">Paiement / Premium</option>
                                <option value="Devenir Ambassadeur">Devenir Ambassadeur</option>
                            </select>
                        </div>
                        <div class="mb-4">
                            <label class="form-label fw-bold">Message</label>
                            <textarea name="message" class="form-control rounded-4 px-4" rows="5" placeholder="Comment pouvons-nous vous aider ?" required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary btn-send w-100 rounded-pill py-3">ENVOYER LE MESSAGE</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
