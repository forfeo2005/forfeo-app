<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><title>Mon Académie | FORFEO</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"></head>
<body class="bg-light">
    <nav class="navbar bg-white border-bottom py-3">
        <div class="container"><a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a><span>Bienvenue, <%= userName %></span></div>
    </nav>
    <div class="container py-5">
        <h2 class="fw-bold mb-4">Mes Formations de Conformité</h2>
        <div class="row g-4">
            <% modules.forEach(m => { %>
                <div class="col-md-4">
                    <div class="card h-100 border-0 shadow-sm rounded-4 p-4 text-center">
                        <h5 class="fw-bold"><%= m.titre %></h5>
                        <p class="small text-muted"><%= m.description %></p>
                        <a href="/formations/module/<%= m.id %>" class="btn btn-primary rounded-pill w-100 mt-auto">Commencer</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
