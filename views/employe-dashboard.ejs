<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Académie | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5">
        
        <div class="d-flex justify-content-between mb-5 align-items-center flex-wrap gap-3">
            <h3 class="fw-bold">Bonjour, <%= userName %> 🎓</h3>
            
            <div class="d-flex flex-column align-items-center">
                <img src="/images/logo-forfeo.png" height="40" alt="Logo">
                <span class="brand-lab-neon">LAB</span>
            </div>
            
            <div class="d-flex gap-2">
                <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill px-3 fw-bold">Mon Profil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-3 fw-bold">Déconnexion</a>
            </div>
        </div>

        <h4 class="fw-bold mb-4 text-primary"><i class="bi bi-collection-play-fill me-2"></i>Mes Formations</h4>
        
        <div class="row g-4">
            <% modules.forEach(m => { 
                // Chercher si l'utilisateur a déjà un score pour ce module
                const score = scores.find(s => s.module_id === m.id);
                
                // Variables par défaut (Non commencé)
                let statusBadge = '<span class="badge bg-light text-dark border">Non commencé</span>';
                let actionButton = `<a href="/formations/module/${m.id}" class="btn btn-primary w-100 btn-sm rounded-pill fw-bold shadow-sm">Commencer</a>`;
                let cardBorder = "";

                // Logique selon le score
                if (score) {
                    if (score.statut === 'reussi') {
                        // SUCCÈS (>80%)
                        statusBadge = `<span class="badge bg-success bg-opacity-10 text-success border border-success">RÉUSSI (${Math.round(score.meilleur_score)}%)</span>`;
                        actionButton = `<a href="/certificat/${score.code_verif}" target="_blank" class="btn btn-outline-success w-100 btn-sm rounded-pill fw-bold"><i class="bi bi-trophy-fill me-1"></i> Certificat</a>`;
                        cardBorder = "border-success border-opacity-25";
                    } else {
                        // ÉCHEC (<80%)
                        statusBadge = `<span class="badge bg-danger bg-opacity-10 text-danger border border-danger">ÉCHOUÉ (${Math.round(score.meilleur_score)}%)</span>`;
                        actionButton = `<a href="/formations/module/${m.id}" class="btn btn-warning w-100 btn-sm rounded-pill fw-bold text-dark shadow-sm"><i class="bi bi-arrow-repeat me-1"></i> Réessayer</a>`;
                        cardBorder = "border-warning border-opacity-25";
                    }
                }
            %>
                <div class="col-md-6 col-lg-4">
                    <div class="glass-card h-100 d-flex flex-column <%= cardBorder %>">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <div class="p-2 bg-primary bg-opacity-10 rounded-circle text-primary">
                                <i class="bi <%= m.image_icon %> fs-4"></i>
                            </div>
                            <%- statusBadge %>
                        </div>
                        
                        <h5 class="fw-bold mb-2"><%= m.titre %></h5>
                        <p class="small text-muted flex-grow-1" style="line-height: 1.5;"><%= m.description %></p>
                        
                        <div class="mt-3 pt-3 border-top d-flex flex-column gap-2">
                            <div class="d-flex justify-content-between align-items-center small text-muted mb-2">
                                <span><i class="bi bi-clock me-1"></i> <%= m.duree %></span>
                                <span><i class="bi bi-award me-1"></i> +80% requis</span>
                            </div>
                            <%- actionButton %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    
    <%- include('partials/forfy') %>
</body>
</html>
