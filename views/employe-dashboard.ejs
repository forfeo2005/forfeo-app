<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Académie | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; }
        .course-card { border: none; border-radius: 20px; transition: 0.3s; }
        .course-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom py-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="d-flex align-items-center">
                <span class="me-3 small fw-bold"><%= userName %></span>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 text-center">
        <h1 class="fw-bold mb-2">Mes Formations Employé</h1>
        <p class="text-muted mb-5">Complétez vos modules obligatoires pour obtenir vos certificats.</p>

        <div class="row justify-content-center g-4">
            <% if (modules.length === 0) { %>
                <div class="col-12"><p class="text-muted">Aucune formation disponible.</p></div>
            <% } else { %>
                <% modules.forEach(m => { %>
                    <div class="col-md-5 col-lg-4">
                        <div class="card h-100 course-card p-4 shadow-sm bg-white">
                            <div class="mb-3 text-primary"><i class="bi bi-mortarboard fs-1"></i></div>
                            <h4 class="fw-bold"><%= m.titre %></h4>
                            <p class="small text-muted mb-4"><%= m.description %></p>
                            <a href="/formations/module/<%= m.id %>" class="btn btn-primary rounded-pill w-100 fw-bold py-2 shadow-sm">
                                Démarrer
                            </a>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
