<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Académie Forfeo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
    <style>
        .course-card { height: 100%; display: flex; flex-direction: column; }
        .course-img { height: 120px; background: linear-gradient(135deg, #e0efff, #f8fafc); border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 3rem; color: var(--primary); margin-bottom: 15px; }
    </style>
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5">
        
        <div class="d-flex justify-content-between align-items-center mb-5 reveal-up">
            <div>
                <h6 class="text-primary fw-bold text-uppercase mb-1">Espace Apprenant</h6>
                <h2 class="fw-bold">Forfeo Académie</h2>
            </div>
            <div class="d-flex align-items-center gap-3">
                <div class="text-end d-none d-md-block">
                    <div class="fw-bold"><%= userName %></div>
                    <div class="small text-muted">Employé Certifié</div>
                </div>
                <a href="/logout" class="btn btn-outline-premium btn-sm">Quitter</a>
            </div>
        </div>

        <div class="glass-card mb-5 bg-white border-primary reveal-up delay-100" style="border-left: 5px solid var(--primary);">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <div class="badge bg-primary-subtle text-primary mb-2">RECOMMANDÉ</div>
                    <h3 class="fw-bold">Excellence du Service à la Clientèle</h3>
                    <p class="text-muted">Maîtrisez les bases universelles pour créer un effet WOW chez vos clients.</p>
                    <div class="d-flex gap-3 text-muted small mb-4">
                        <span><i class="bi bi-clock"></i> 30 min</span>
                        <span><i class="bi bi-book"></i> 6 leçons</span>
                        <span><i class="bi bi-trophy"></i> Certificat</span>
                    </div>
                    <a href="/formations/module/1" class="btn btn-premium px-5">Commencer</a>
                </div>
                <div class="col-md-4 text-center d-none d-md-block">
                    <i class="bi bi-stars text-primary opacity-25" style="font-size: 8rem;"></i>
                </div>
            </div>
        </div>

        <h4 class="fw-bold mb-4 reveal-up delay-200">Catalogue de formations</h4>
        <div class="row g-4">
            <% modules.forEach((m, index) => { %>
                <div class="col-md-4 reveal-up delay-<%= (index+2)*100 %>">
                    <div class="glass-card course-card p-3">
                        <div class="course-img">
                            <% if(index === 0) { %><i class="bi bi-emoji-smile"></i><% } %>
                            <% if(index === 1) { %><i class="bi bi-chat-heart"></i><% } %>
                            <% if(index === 2) { %><i class="bi bi-shield-check"></i><% } %>
                            <% if(index === 3) { %><i class="bi bi-bar-chart"></i><% } %>
                            <% if(index === 4) { %><i class="bi bi-people"></i><% } %>
                        </div>
                        <h6 class="fw-bold mb-2"><%= m.titre %></h6>
                        <p class="small text-muted mb-3 flex-grow-1"><%= m.description %></p>
                        <div class="d-flex justify-content-between align-items-center mt-auto pt-3 border-top border-light">
                            <small class="text-muted"><%= m.duree || '30 min' %></small>
                            <a href="/formations/module/<%= m.id %>" class="btn btn-sm btn-outline-premium rounded-pill">Démarrer</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="/js/premium-ui.js"></script>
</body>
</html>
