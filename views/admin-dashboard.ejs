<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <title>FORFEO LAB - Contrôle des Candidatures</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #0f172a; color: white; font-family: 'Inter', sans-serif; padding: 60px 0; }
        .table-dark-custom { background: #1e293b; border-radius: 20px; overflow: hidden; }
        .table thead { background: #0f172a; color: #94a3b8; font-size: 0.8rem; text-transform: uppercase; }
        .btn-approve { background: #2563eb; color: white; border: none; border-radius: 8px; padding: 5px 15px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="fw-bold mb-5 display-4 text-center">Contrôle des Candidatures</h1>
        <div class="table-dark-custom p-4 shadow-lg">
            <table class="table table-dark table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th class="ps-4">Mission</th>
                        <th>Ambassadeur</th>
                        <th>Statut</th>
                        <th class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (candidatures.length === 0) { %>
                        <tr><td colspan="4" class="text-center py-5 text-muted">Aucune candidature en attente.</td></tr>
                    <% } %>
                    <% candidatures.forEach(c => { %>
                    <tr>
                        <td class="ps-4 fw-bold"><%= c.titre %></td>
                        <td><%= c.ambassadeur %></td>
                        <td><span class="badge bg-warning text-dark"><%= c.statut %></span></td>
                        <td class="text-center">
                            <form action="/admin/approuver" method="POST">
                                <input type="hidden" name="id" value="<%= c.id %>">
                                <button type="submit" class="btn-approve">Approuver</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <div class="text-center mt-5">
            <a href="/logout" class="btn btn-outline-danger rounded-pill px-4">Déconnexion sécurisée</a>
        </div>
    </div>
</body>
</html>
