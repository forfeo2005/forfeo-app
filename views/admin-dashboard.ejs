<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <title>FORFEO ADMIN - Contrôle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #0f172a; color: white; font-family: 'Inter', sans-serif; }
        .card { background: #1e293b; border: none; color: white; border-radius: 15px; }
        .table { color: white; }
    </style>
</head>
<body class="p-5">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h1 class="fw-bold">Tableau de Bord Admin</h1>
            <a href="/logout" class="btn btn-danger">Déconnexion</a>
        </div>

        <div class="row g-4">
            <div class="col-md-6">
                <div class="card p-4">
                    <h3>Entreprises inscrites</h3>
                    <p class="display-4"><%= entreprises.length %></p>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card p-4">
                    <h3>Ambassadeurs</h3>
                    <p class="display-4"><%= ambassadeurs.length %></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
