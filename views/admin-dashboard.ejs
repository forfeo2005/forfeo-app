<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Admin 🛡️ | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body { background: #f1f5f9; font-family: 'Inter', sans-serif; }
        .sidebar { background: #0f172a; min-height: 100vh; color: white; padding: 2rem; position: sticky; top: 0; }
        .glass-card { background: white; border: none; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.04); padding: 25px; margin-bottom: 2rem; }
        .star-rating { color: #ffc107; font-size: 0.9rem; }
        .status-badge { padding: 5px 12px; border-radius: 50px; font-weight: 700; font-size: 10px; text-transform: uppercase; }
        .bg-approuve { background: #dcfce7; color: #166534; }
        .bg-termine { background: #e0f2fe; color: #075985; }
    </style>
</head>
<body>

<div class="container-fluid">
    <div class="row">
        <nav class="col-md-2 sidebar d-none d-md-block shadow-lg">
            <h4 class="fw-bold mb-5">FORFEO <span class="text-primary">LAB</span></h4>
            <ul class="nav flex-column gap-3">
                <li class="nav-item"><a href="/admin/dashboard" class="nav-link text-white"><i class="bi bi-grid-fill me-2"></i> Dashboard</a></li>
                <li class="nav-item"><a href="/logout" class="nav-link text-danger"><i class="bi bi-power me-2"></i> Déconnexion</a></li>
            </ul>
        </nav>

        <main class="col-md-10 p-5">
            <div class="d-flex justify-content-between align-items-center mb-5">
                <h2 class="fw-bold m-0">Performance du Réseau</h2>
                <div class="bg-white px-4 py-2 rounded-pill shadow-sm small fw-bold"><%= userName %></div>
            </div>

            <div class="row g-4 mb-5">
                <div class="col-lg-8">
                    <div class="glass-card">
                        <h6 class="fw-bold mb-4">REVENUS MENSUELS ($ CAD)</h6>
                        <canvas id="revenueChart" height="250"></canvas>
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="glass-card text-center mb-4">
                        <h6 class="text-muted small fw-bold">ENTREPRISES</h6>
                        <h2 class="fw-bold text-primary m-0"><%= entreprises.length %></h2>
                    </div>
                    <div class="glass-card text-center">
                        <h6 class="text-muted small fw-bold">AUDITS VALIDÉS</h6>
                        <h2 class="fw-bold m-0"><%= rapports.filter(r => r.statut === 'approuve').length %></h2>
                    </div>
                </div>
            </div>

            <div class="glass-card">
                <h5 class="fw-bold mb-4">Missions en attente de validation</h5>
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead class="text-muted small">
                            <tr><th>ENTREPRISE</th><th>MISSION</th><th>RÉSULTAT</th><th>NOTE</th><th class="text-end">ACTION</th></tr>
                        </thead>
                        <tbody>
                            <% rapports.forEach(r => { %>
                                <tr>
                                    <td><strong><%= r.entreprise_nom %></strong></td>
                                    <td class="small text-muted"><%= r.titre %></td>
                                    <td>
                                        <span class="status-badge bg-<%= r.statut %>"><%= r.statut %></span>
                                        <% if(r.photo_preuve) { %>
                                            <a href="<%= r.photo_preuve %>" target="_blank" class="ms-2 btn btn-xs btn-outline-info rounded-pill py-0 px-2" style="font-size: 9px;">Photo</a>
                                        <% } %>
                                    </td>
                                    <td class="star-rating">
                                        <% if(r.note_audit > 0) { %>
                                            <% for(let i=0; i<r.note_audit; i++) { %><i class="bi bi-star-fill"></i><% } %>
                                        <% } else { %> - <% } %>
                                    </td>
                                    <td class="text-end">
                                        <% if(r.statut === 'termine') { %>
                                            <form action="/admin/approuver-audit" method="POST" class="d-flex align-items-center justify-content-end gap-2">
                                                <input type="hidden" name="id_mission" value="<%= r.id %>">
                                                <select name="note_audit" class="form-select form-select-sm rounded-pill w-auto" required>
                                                    <option value="5">⭐⭐⭐⭐⭐ (5)</option>
                                                    <option value="4">⭐⭐⭐⭐ (4)</option>
                                                    <option value="3">⭐⭐⭐ (3)</option>
                                                    <option value="2">⭐⭐ (2)</option>
                                                    <option value="1">⭐ (1)</option>
                                                </select>
                                                <button class="btn btn-primary btn-sm rounded-pill px-3 fw-bold shadow-sm">Valider</button>
                                            </form>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</div>

<script>
    new Chart(document.getElementById('revenueChart'), {
        type: 'line',
        data: {
            labels: <%- JSON.stringify(chartData.map(d => d.mois)) %>,
            datasets: [{
                label: 'Revenus ($)',
                data: <%- JSON.stringify(chartData.map(d => d.total)) %>,
                borderColor: '#0d6efd',
                backgroundColor: 'rgba(13, 110, 253, 0.05)',
                fill: true, tension: 0.4, borderWidth: 3
            }]
        },
        options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true } } }
    });
</script>
</body>
</html>
