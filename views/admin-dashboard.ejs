<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Console Admin | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7fb; color: #334155; }
        .sidebar { background: #1e293b; min-height: 100vh; color: white; padding: 2rem 1.5rem; }
        .nav-link { color: #94a3b8; font-weight: 500; border-radius: 8px; padding: 0.8rem 1rem; transition: 0.3s; }
        .nav-link:hover, .nav-link.active { background: #334155; color: white; }
        
        .main-content { padding: 3rem; }
        .stat-card { border: none; border-radius: 16px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); transition: 0.3s; }
        .stat-card:hover { transform: translateY(-5px); }
        
        .table-container { background: white; border-radius: 16px; padding: 1.5rem; box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1); }
        .table thead { background: #f8fafc; }
        .table th { border: none; color: #64748b; text-transform: uppercase; font-size: 0.75rem; letter-spacing: 0.05em; padding: 1rem; }
        .table td { vertical-align: middle; padding: 1rem; border-color: #f1f5f9; }
        
        .status-badge { padding: 0.4rem 0.8rem; border-radius: 50px; font-size: 0.75rem; font-weight: 700; }
        .bg-disponible { background: #fef9c3; color: #854d0e; }
        .bg-reserve { background: #dcfce7; color: #166534; }
        .bg-termine { background: #e0f2fe; color: #075985; }
        .bg-approuve { background: #f1f5f9; color: #475569; border: 1px solid #cbd5e1; }

        .btn-action { border-radius: 8px; font-weight: 600; font-size: 0.85rem; padding: 0.5rem 1rem; }
    </style>
</head>
<body>

<div class="container-fluid">
    <div class="row">
        <nav class="col-md-3 col-lg-2 sidebar d-md-block collapse">
            <div class="mb-5 px-3">
                <h4 class="fw-bold text-white mb-0">FORFEO <span class="text-primary">ADMIN</span></h4>
                <p class="small text-muted">Console de gestion v1.0</p>
            </div>
            <ul class="nav flex-column gap-2">
                <li class="nav-item"><a href="/admin/dashboard" class="nav-link active"><i class="bi bi-speedometer2 me-2"></i> Vue d'ensemble</a></li>
                <li class="nav-item"><a href="/logout" class="nav-link text-danger mt-5"><i class="bi bi-box-arrow-left me-2"></i> Déconnexion</a></li>
            </ul>
        </nav>

        <main class="col-md-9 ms-sm-auto col-lg-10 main-content">
            <div class="d-flex justify-content-between align-items-center mb-5">
                <h2 class="fw-bold mb-0">Tableau de Bord Administrateur 🛡️</h2>
                <div class="text-end">
                    <span class="small text-muted">Bonjour,</span>
                    <p class="fw-bold mb-0"><%= userName %></p>
                </div>
            </div>

            <div class="row g-4 mb-5">
                <div class="col-md-4">
                    <div class="card stat-card p-4">
                        <div class="d-flex justify-content-between">
                            <div>
                                <p class="text-muted small fw-bold mb-1">ENTREPRISES</p>
                                <h3 class="fw-bold mb-0"><%= entreprises.length %></h3>
                            </div>
                            <div class="bg-primary bg-opacity-10 p-3 rounded-circle text-primary"><i class="bi bi-building fs-4"></i></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card stat-card p-4">
                        <div class="d-flex justify-content-between">
                            <div>
                                <p class="text-muted small fw-bold mb-1">AUDITS TOTAUX</p>
                                <h3 class="fw-bold mb-0"><%= rapports.length %></h3>
                            </div>
                            <div class="bg-info bg-opacity-10 p-3 rounded-circle text-info"><i class="bi bi-clipboard-check fs-4"></i></div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="table-container">
                <h5 class="fw-bold mb-4">Flux d'Audits Mystères au Québec</h5>
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>Entreprise</th>
                                <th>Mission</th>
                                <th>Statut</th>
                                <th>Ambassadeur</th>
                                <th>Rapport</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% rapports.forEach(r => { %>
                                <tr>
                                    <td><span class="fw-bold"><%= r.entreprise_nom %></span></td>
                                    <td class="text-muted"><%= r.titre %></td>
                                    <td>
                                        <span class="status-badge bg-<%= r.statut %>">
                                            <%= r.statut %>
                                        </span>
                                    </td>
                                    <td>
                                        <% if(r.ambassadeur_id) { %>
                                            <span class="small fw-bold text-dark"><i class="bi bi-person me-1"></i> ID: <%= r.ambassadeur_id %></span>
                                        <% } else { %>
                                            <span class="small text-muted">En attente</span>
                                        <% } %>
                                    </td>
                                    <td class="small">
                                        <% if(r.rapport_final) { %>
                                            <span class="text-success"><i class="bi bi-file-earmark-check me-1"></i> Soumis</span>
                                        <% } else { %>
                                            <span class="text-muted">Aucun</span>
                                        <% } %>
                                    </td>
                                    <td class="text-end">
                                        <div class="d-flex justify-content-end gap-2">
                                            <form action="/admin/approuver-audit" method="POST">
                                                <input type="hidden" name="id_mission" value="<%= r.id %>">
                                                <button type="submit" class="btn btn-sm btn-outline-success btn-action">Approuver</button>
                                            </form>
                                            <form action="/admin/supprimer-audit" method="POST" onsubmit="return confirm('Attention : Cette action est irréversible. Supprimer ?');">
                                                <input type="hidden" name="id_mission" value="<%= r.id %>">
                                                <button type="submit" class="btn btn-sm btn-outline-danger btn-action"><i class="bi bi-trash"></i></button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
