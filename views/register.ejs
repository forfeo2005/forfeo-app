<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Inscription | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5" style="max-width: 500px;">
        <div class="card p-4 shadow border-0 rounded-4">
            <h2 class="fw-bold text-center mb-4">Créer un compte</h2>
            <form action="/register" method="POST">
                <div class="mb-3">
                    <label class="form-label small fw-bold">NOM COMPLET</label>
                    <input type="text" name="nom" class="form-control rounded-3" placeholder="Ex: Jean Tremblay" required>
                </div>
                <div class="mb-3">
                    <label class="form-label small fw-bold">COURRIEL</label>
                    <input type="email" name="email" class="form-control rounded-3" required>
                </div>
                <div class="mb-3">
                    <label class="form-label small fw-bold">MOT DE PASSE</label>
                    <input type="password" name="password" class="form-control rounded-3" required>
                </div>
                <div class="mb-4">
                    <label class="form-label small fw-bold">JE SUIS UN(E) :</label>
                    <select name="role" class="form-select rounded-3">
                        <option value="ambassadeur" <%= role === 'ambassadeur' ? 'selected' : '' %>>Ambassadeur (Testeur)</option>
                        <option value="entreprise" <%= role === 'entreprise' ? 'selected' : '' %>>Entreprise</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold">CRÉER MON COMPTE</button>
            </form>
        </div>
    </div>
    </body>
</html>
