<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; }
        .register-card { border: none; border-radius: 25px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); }
        .form-control { border-radius: 12px; padding: 12px; border: 1px solid #e2e8f0; }
        .btn-register { border-radius: 12px; padding: 12px; font-weight: 600; transition: 0.3s; }
    </style>
</head>
<body>
    <nav class="navbar navbar-light bg-white border-bottom py-3">
        <div class="container">
            <a href="/" class="btn btn-outline-secondary rounded-pill btn-sm"><i class="bi bi-house-door"></i> Accueil</a>
            <a class="navbar-brand fw-bold text-primary mx-auto" href="/">FORFEO LAB</a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="card register-card p-4 p-md-5 bg-white">
                    <h2 class="fw-bold text-center mb-4">
                        <% if (role === 'entreprise') { %> Inscrire mon entreprise <% } else { %> Devenir Ambassadeur <% } %>
                    </h2>

                    <% if (error) { %>
                        <div class="alert alert-danger border-0 rounded-4 small mb-4 text-center">
                            <i class="bi bi-exclamation-circle me-2"></i> <%= error %>
                        </div>
                    <% } %>

                    <form action="/register" method="POST">
                        <input type="hidden" name="role" value="<%= role %>">

                        <div class="mb-3">
                            <label class="small fw-bold text-muted mb-2 text-uppercase">Nom Complet ou Nom d'entreprise</label>
                            <input type="text" name="nom" class="form-control" placeholder="Ex: Jean Tremblay ou ABC Inc." required>
                        </div>

                        <div class="mb-3">
                            <label class="small fw-bold text-muted mb-2 text-uppercase">Adresse Courriel</label>
                            <input type="email" name="email" class="form-control" placeholder="nom@exemple.com" required>
                        </div>

                        <div class="mb-4">
                            <label class="small fw-bold text-muted mb-2 text-uppercase">Mot de passe</label>
                            <input type="password" name="password" class="form-control" placeholder="••••••••" required>
                        </div>

                        <button type="submit" class="btn btn-primary btn-register w-100 shadow-sm mb-3">
                            Créer mon compte
                        </button>
                    </form>

                    <p class="text-center text-muted small">
                        Déjà un compte ? <a href="/login" class="text-primary fw-bold text-decoration-none">Se connecter</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
