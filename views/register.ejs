<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; font-family: 'Inter', sans-serif; }
        .nav-black { background-color: #1a1a1a; padding: 15px 0; }
        .register-card { border-radius: 25px; border: none; box-shadow: 0 15px 35px rgba(0,0,0,0.1); background: white; padding: 40px; margin-top: 50px; }
        .btn-register { background: #0d6efd; border: none; border-radius: 50px; padding: 12px; font-weight: bold; width: 100%; color: white; transition: 0.3s; }
        .btn-register:hover { background: #0b5ed7; transform: translateY(-2px); }
        .form-control { border-radius: 50px; padding: 12px 20px; border: 1px solid #dee2e6; }
    </style>
</head>
<body>

    <nav class="nav-black shadow-sm">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/" class="text-white text-decoration-none fw-bold fs-4">FORFEO <span class="text-primary">LAB</span></a>
            <div class="d-flex gap-4">
                <a href="/login" class="text-white text-decoration-none small">Déjà inscrit ? Connexion</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="register-card">
                    <div class="text-center mb-4">
                        <h2 class="fw-bold">Créer un compte</h2>
                        <p class="text-muted">Rejoignez l'excellence au Québec</p>
                    </div>

                    <form action="/register" method="POST">
                        <div class="mb-3">
                            <label class="small fw-bold mb-2">NOM COMPLET</label>
                            <input type="text" name="nom" class="form-control" placeholder="Ex: Jean Tremblay" required>
                        </div>

                        <div class="mb-3">
                            <label class="small fw-bold mb-2">COURRIEL</label>
                            <input type="email" name="email" class="form-control" placeholder="votre@email.com" required>
                        </div>

                        <div class="mb-3">
                            <label class="small fw-bold mb-2">MOT DE PASSE</label>
                            <input type="password" name="password" class="form-control" placeholder="••••••••" required>
                        </div>

                        <div class="mb-4">
                            <label class="small fw-bold mb-2">JE SUIS UN(E) :</label>
                            <select name="role" class="form-select rounded-pill px-3" style="padding: 12px;">
                                <option value="ambassadeur">Ambassadeur (Testeur)</option>
                                <option value="entreprise">Entreprise (Client)</option>
                            </select>
                        </div>

                        <button type="submit" class="btn-register shadow">CRÉER MON COMPTE</button>
                    </form>

                    <div class="text-center mt-4">
                        <p class="small text-muted">En vous inscrivant, vous acceptez nos conditions d'utilisation.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
