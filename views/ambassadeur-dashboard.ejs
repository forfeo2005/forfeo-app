<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Missions Québec | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f4f7fb; font-family: 'Inter', sans-serif; }
        .mission-card { border: none; border-radius: 20px; transition: 0.3s; background: white; }
        .mission-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .reward { background: #dcfce7; color: #166534; font-weight: 800; padding: 5px 15px; border-radius: 50px; }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <div class="d-flex align-items-center gap-3">
            <a href="/ambassadeur/mes-missions" class="btn btn-primary rounded-pill btn-sm px-3">Mes Missions</a>
            <span class="fw-bold"><%= userName %></span>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Quitter</a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row mb-5 align-items-center">
            <div class="col-md-8">
                <h2 class="fw-bold">Missions disponibles au Québec 📍</h2>
                <p class="text-muted">Gagnez de l'argent en évaluant des commerces locaux.</p>
            </div>
            <div class="col-md-4 text-md-end">
                <div class="bg-white p-3 rounded-4 shadow-sm border-bottom border-success border-4">
                    <small class="text-muted fw-bold">VOS GAINS</small>
                    <h3 class="fw-bold text-success mb-0"><%= totalGains %>$ CAD</h3>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <% if(missions.length === 0) { %>
                <div class="col-12 text-center py-5 text-muted">Aucune nouvelle mission disponible.</div>
            <% } %>
            <% missions.forEach(m => { %>
                <div class="col-md-6 col-lg-4">
                    <div class="card mission-card p-4 h-100 shadow-sm">
                        <div class="d-flex justify-content-between mb-3">
                            <span class="badge bg-light text-primary border">Audit</span>
                            <span class="reward"><%= m.recompense %>$</span>
                        </div>
                        <h5 class="fw-bold"><%= m.titre %></h5>
                        <p class="small text-muted mb-4"><%= m.description.split('---')[1]?.substring(0, 100) || m.description.substring(0, 100) %>...</p>
                        <form action="/postuler-mission" method="POST" class="mt-auto">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Réserver</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
