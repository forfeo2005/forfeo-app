<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8f9fa; font-family: 'Inter', sans-serif; }
        .card-mission { border: none; border-radius: 20px; transition: transform 0.2s; }
        .card-mission:hover { transform: translateY(-5px); }
        .stats-badge { background: linear-gradient(135deg, #0d6efd, #00d2ff); color: white; border-radius: 15px; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom py-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="d-flex align-items-center">
                <a href="/profil" class="btn btn-outline-secondary btn-sm rounded-pill me-2">Mon Profil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row align-items-center mb-5">
            <div class="col-md-8">
                <h1 class="fw-bold">Bonjour, <%= userName %> 👋</h1>
                <p class="text-muted">Prêt pour votre prochaine mission d'audit ?</p>
            </div>
            <div class="col-md-4 text-end">
                <div class="p-4 stats-badge shadow-sm">
                    <small class="text-uppercase opacity-75">Mes Gains Totaux</small>
                    <h2 class="fw-bold m-0"><%= totalGains %>$ CAD</h2>
                </div>
            </div>
        </div>

        <h3 class="fw-bold mb-4">Missions disponibles au Québec</h3>
        <div class="row g-4">
            <% if (missions.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <p class="text-muted">Aucune mission disponible pour le moment.</p>
                </div>
            <% } else { %>
                <% missions.forEach(m => { %>
                    <div class="col-md-4">
                        <div class="card h-100 card-mission shadow-sm p-4">
                            <span class="badge bg-primary-subtle text-primary rounded-pill mb-3 w-fit">Audit Mystère</span>
                            <h5 class="fw-bold"><%= m.titre %></h5>
                            <p class="small text-muted flex-grow-1"><%= m.description %></p>
                            <div class="d-flex justify-content-between align-items-center mt-3 pt-3 border-top">
                                <span class="fw-bold text-success fs-5"><%= m.recompense %>$</span>
                                <form action="/postuler-mission" method="POST">
                                    <input type="hidden" name="id_mission" value="<%= m.id %>">
                                    <button type="submit" class="btn btn-primary rounded-pill px-4 fw-bold shadow-sm">Postuler</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
</body>
</html>
