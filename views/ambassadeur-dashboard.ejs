<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Missions | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>body { background: #f4f7fb; font-family: 'Inter', sans-serif; }</style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <div class="d-flex align-items-center gap-3">
            <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill px-3">Mon Profil</a>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill"><i class="bi bi-box-arrow-right"></i></a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold m-0">Gains : <%= totalGains %>$</h2>
            <a href="/ambassadeur/mes-missions" class="btn btn-primary rounded-pill">Mes Missions</a>
        </div>
        <div class="row g-4">
            <% missions.forEach(m => { %>
                <div class="col-md-6">
                    <div class="card p-4 border-0 shadow-sm rounded-4" style="border-left: 5px solid #007bff !important;">
                        <div class="d-flex justify-content-between mb-3">
                            <h5 class="fw-bold m-0"><%= m.titre %></h5>
                            <span class="badge bg-success rounded-pill px-3"><%= m.recompense %></span>
                        </div>
                        <p class="text-muted small"><%= m.description %></p>
                        <form action="/postuler-mission" method="POST">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button type="submit" class="btn btn-outline-primary w-100 rounded-pill py-2 fw-bold">Réserver</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <% if (showWelcome) { %>
    <div class="modal fade" id="welcomeModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered text-center">
            <div class="modal-content border-0 shadow-lg rounded-4 p-5">
                <i class="bi bi-currency-dollar text-success mb-4" style="font-size: 4rem;"></i>
                <h2 class="fw-bold mb-3">Salut <%= userName %> ! 🕵️‍♂️</h2>
                <p class="text-muted mb-4">Prêt à gagner vos premiers dollars ? Explorez les missions d'audit mystère disponibles au Québec dès maintenant.</p>
                <button type="button" class="btn btn-primary rounded-pill px-5 py-2 fw-bold" data-bs-dismiss="modal">Découvrir les missions</button>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>document.addEventListener('DOMContentLoaded', () => { new bootstrap.Modal(document.getElementById('welcomeModal')).show(); });</script>
    <% } %>
</body>
</html>
