<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Espace Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .mission-card { border: none; border-radius: 20px; transition: 0.3s; background: white; }
        .mission-card:hover { transform: translateY(-5px); box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
        .reward-badge { background: #eef6ff; color: #0d6efd; padding: 5px 15px; border-radius: 50px; font-weight: bold; }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-white bg-white border-bottom p-3">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">FORFEO <span class="text-primary">LAB</span></a>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Quitter</a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="mb-5">
            <h1 class="fw-bold">Missions disponibles au Québec 📍</h1>
            <p class="text-muted">Sélectionnez une mission et commencez votre audit.</p>
        </div>

        <div class="row g-4">
            <% missions.forEach(m => { %>
                <div class="col-md-6">
                    <div class="card mission-card p-4 h-100">
                        <div class="d-flex justify-content-between mb-3">
                            <h4 class="fw-bold mb-0"><%= m.titre %></h4>
                            <span class="reward-badge"><%= m.recompense %></span>
                        </div>
                        <p class="text-muted"><%= m.description %></p>
                        <div class="mt-auto">
                            <button class="btn btn-dark w-100 rounded-pill">Postuler pour cet audit</button>
                        </div>
                    </div>
                </div>
            <% }) %>
            <% if (missions.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <p class="text-muted">Aucune mission disponible pour le moment. Forfy vous préviendra !</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
