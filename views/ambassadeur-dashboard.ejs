<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Portail Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .mission-card { transition: 0.3s; border-radius: 15px; border-left: 5px solid #28a745; }
        .mission-card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#">FORFEO <span class="text-success">AMBASSADEUR</span></a>
            <a href="/logout" class="btn btn-outline-light btn-sm">Déconnexion</a>
        </div>
    </nav>

    <div class="container py-5">
        <h2 class="fw-bold mb-4">Missions Disponibles</h2>
        <div class="row g-4">
            <% missions.forEach(mission => { %>
                <div class="col-md-6 col-lg-4">
                    <div class="card mission-card p-4 shadow-sm border-0 h-100">
                        <span class="badge bg-success-subtle text-success mb-2 w-fit">Nouvelle Mission</span>
                        <h5 class="fw-bold"><%= mission.titre %></h5>
                        <p class="small text-muted"><%= mission.description %></p>
                        <hr>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-primary">🎁 <%= mission.recompense %></span>
                            <button class="btn btn-primary btn-sm rounded-pill">Postuler</button>
                        </div>
                    </div>
                </div>
            <% }) %>
            <% if (missions.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <p class="text-muted">Aucune mission n'est disponible pour le moment. Reviens plus tard !</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
