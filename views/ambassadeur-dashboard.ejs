<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ambassadeur | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/premium-dashboard.css">
</head>
<body>
    <div class="premium-bg"></div>
    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h3 class="fw-bold">Bonjour, <%= userName %> 👋</h3>
            <div class="d-flex gap-2">
                <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill">Mon Profil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill">Déconnexion</a>
            </div>
        </div>

        <div class="glass-card bg-primary text-white mb-5 p-4" style="background: linear-gradient(135deg, #0061ff, #3b82f6);">
            <div class="opacity-75 small fw-bold uppercase mb-1">Mes Gains Totaux</div>
            <div class="display-3 fw-bold"><%= totalGains %> $ CAD</div>
        </div>

        <h4 class="fw-bold mb-4">Missions disponibles au Québec</h4>
        <div class="row g-4 mb-5">
            <% if(missions.length === 0) { %><div class="col-12 text-muted">Aucune mission pour le moment.</div><% } %>
            <% missions.forEach(m => { %>
                <div class="col-md-4">
                    <div class="glass-card h-100">
                        <div class="d-flex justify-content-between mb-3">
                            <span class="badge bg-primary-subtle text-primary"><%= m.type_audit %></span>
                            <span class="fw-bold text-success"><%= m.recompense %>$</span>
                        </div>
                        <h5 class="fw-bold"><%= m.titre %></h5>
                        <p class="small text-muted"><%= m.description %></p>
                        <form action="/ambassadeur/postuler" method="POST">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button class="btn btn-primary w-100 rounded-pill">Réserver</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
        
        <div class="glass-card">
            <h5 class="fw-bold mb-4">Historique de mes missions</h5>
            <table class="premium-table">
                <thead><tr><th>Mission</th><th>Type</th><th>Statut</th><th>Action</th></tr></thead>
                <tbody>
                    <% historique.forEach(h => { %>
                        <tr>
                            <td class="fw-bold"><%= h.titre %></td>
                            <td class="small text-muted"><%= h.type_audit %></td>
                            <td><span class="badge-status <%= h.statut %>"><%= h.statut %></span></td>
                            <td>
                                <% if(h.statut === 'reserve') { %>
                                    
                                    <% if(h.type_audit.includes('Sondage')) { %>
                                        <div class="d-flex gap-2">
                                            <a href="mailto:<%= h.client_email %>?subject=Votre avis compte - Enquête de satisfaction&body=Bonjour <%= h.client_nom %>,%0D%0A%0D%0AJe vous contacte au nom de Forfeo Lab, une entreprise québécoise spécialisée dans la qualité de service.%0D%0A%0D%0ANous aimerions recueillir vos impressions suite à votre récente expérience.%0D%0A%0D%0ACordialement,%0D%0AL'équipe Forfeo." class="btn btn-sm btn-info text-white rounded-pill">
                                                <i class="bi bi-envelope"></i> Contacter
                                            </a>
                                            <button class="btn btn-sm btn-primary rounded-pill" data-bs-toggle="modal" data-bs-target="#auditModal<%= h.id %>">
                                                Noter Réponses
                                            </button>
                                        </div>
                                    <% } else { %>
                                        <button class="btn btn-sm btn-primary rounded-pill px-4" data-bs-toggle="modal" data-bs-target="#auditModal<%= h.id %>">
                                            Remplir Rapport
                                        </button>
                                    <% } %>

                                    <div class="modal fade" id="auditModal<%= h.id %>" tabindex="-1">
                                        <div class="modal-dialog modal-lg">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title">Rapport : <%= h.titre %></h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                </div>
                                                <div class="modal-body">
                                                    <form action="/ambassadeur/soumettre-rapport" method="POST">
                                                        <input type="hidden" name="mission_id" value="<%= h.id %>">
                                                        
                                                        <div class="alert alert-info">
                                                            <% if(h.type_audit.includes('Sondage')) { %>
                                                                Veuillez transcrire les réponses fournies par le client <strong><%= h.client_nom %></strong>.
                                                            <% } else { %>
                                                                Veuillez évaluer votre visite.
                                                            <% } %>
                                                        </div>

                                                        <div class="audit-form-section">
                                                            <label class="fw-bold mb-2">1. Note Globale / Satisfaction</label>
                                                            <select name="q1" class="form-select" required>
                                                                <option value="5">5 - Très Satisfait / Excellent</option>
                                                                <option value="4">4 - Satisfait / Bon</option>
                                                                <option value="3">3 - Neutre / Moyen</option>
                                                                <option value="2">2 - Insatisfait</option>
                                                                <option value="1">1 - Très insatisfait</option>
                                                            </select>
                                                        </div>
                                                        <div class="audit-form-section">
                                                            <label class="fw-bold mb-2">2. Recommandation (NPS)</label>
                                                            <div class="d-flex gap-3">
                                                                <div class="form-check"><input type="radio" name="q2" value="Oui" class="form-check-input" required> Recommanderait</div>
                                                                <div class="form-check"><input type="radio" name="q2" value="Peut-être" class="form-check-input"> Peut-être</div>
                                                                <div class="form-check"><input type="radio" name="q2" value="Non" class="form-check-input"> Non</div>
                                                            </div>
                                                        </div>
                                                        <div class="audit-form-section">
                                                            <label class="fw-bold mb-2">3. Commentaires / Verbatim</label>
                                                            <textarea name="commentaire" class="form-control" rows="3" required placeholder="Détails de l'expérience..."></textarea>
                                                        </div>
                                                        <button type="submit" class="btn btn-success w-100 rounded-pill">Soumettre le Rapport</button>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                <% } else if(h.statut === 'soumis') { %>
                                    <span class="text-muted small">En attente validation</span>
                                <% } else { %>
                                    <i class="bi bi-check-circle-fill text-success"></i> Terminé
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
