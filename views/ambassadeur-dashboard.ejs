<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Espace Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .mission-card { border: none; border-radius: 20px; transition: 0.3s; background: white; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .mission-card:hover { transform: translateY(-5px); box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
        .reward-badge { background: #eef6ff; color: #0d6efd; padding: 5px 15px; border-radius: 50px; font-weight: bold; }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-white bg-white border-bottom p-3">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">FORFEO <span class="text-primary">LAB</span></a>
            <div class="d-flex align-items-center">
                <span class="me-3 small fw-bold">Ambassadeur: <%= userName %></span>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Quitter</a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="mb-5">
            <h1 class="fw-bold">Missions disponibles au Québec 📍</h1>
            <p class="text-muted">Sélectionnez une mission et commencez votre audit.</p>
        </div>

        <div class="row g-4">
            <% missions.forEach(m => { %>
                <div class="col-md-6">
                    <div class="card mission-card p-4 h-100 d-flex flex-column">
                        <div class="d-flex justify-content-between mb-3 align-items-center">
                            <h4 class="fw-bold mb-0 text-uppercase"><%= m.titre %></h4>
                            <span class="reward-badge"><%= m.recompense %></span>
                        </div>
                        <p class="text-muted"><%= m.description %></p>
                        
                        <div class="mt-auto pt-3">
                            <form action="/postuler-mission" method="POST">
                                <input type="hidden" name="id_mission" value="<%= m.id %>">
                                <button type="submit" class="btn btn-dark w-100 rounded-pill py-2 fw-bold shadow-sm">
                                    Postuler pour cet audit
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }) %>

            <% if (missions.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <img src="/ForfyLogo.png" style="width:80px; opacity:0.3;" class="mb-3">
                    <p class="text-muted fs-5">Aucune nouvelle mission disponible pour le moment.<br>Forfy vous préviendra dès qu'un nouvel audit sera publié !</p>
                </div>
            <% } %>
        </div>
    </div>

    <div id="forfy-btn" style="position:fixed; bottom:25px; right:25px; cursor:pointer; z-index:1000;">
        <img src="/ForfyLogo.png" alt="Forfy" style="width:65px; filter:drop-shadow(0 5px 15px rgba(0,0,0,0.2));">
    </div>

    <div id="forfy-chat" style="display:none; position:fixed; bottom:100px; right:25px; width:320px; height:450px; background:white; border-radius:20px; box-shadow:0 15px 50px rgba(0,0,0,0.15); z-index:1000; overflow:hidden; border:1px solid #eee;">
        <div style="background:#0d6efd; color:white; padding:15px; font-weight:bold;">Forfy - Assistant FORFEO</div>
        <div id="forfy-msgs" style="height:330px; padding:15px; overflow-y:auto; font-size:0.9rem;">
            <div class="mb-2"><b>Forfy:</b> Prêt pour un audit au Québec ? Je peux t'aider à choisir !</div>
        </div>
        <div style="padding:10px; border-top:1px solid #eee;">
            <input type="text" id="forfy-input" placeholder="Posez une question..." style="width:100%; border-radius:50px; border:1px solid #ddd; padding:10px 15px; outline:none;">
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
