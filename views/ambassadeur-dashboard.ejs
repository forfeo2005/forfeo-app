<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FORFEO LAB - Portail Ambassadeur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body { background-color: #f1f5f9; font-family: 'Inter', sans-serif; }
        .nav-amb { background: #0f172a; padding: 15px 0; color: white; }
        .mission-card { background: white; border-radius: 20px; border: none; transition: 0.3s; overflow: hidden; }
        .mission-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.1); }
        .badge-reward { background: #dcfce7; color: #166534; font-weight: bold; border-radius: 8px; padding: 5px 12px; }
        .btn-apply { background: #2563eb; color: white; border-radius: 12px; font-weight: bold; width: 100%; border: none; padding: 10px; }
    </style>
</head>
<body>
    <nav class="nav-amb shadow-sm mb-5">
        <div class="container d-flex justify-content-between align-items-center">
            <h5 class="fw-bold text-primary mb-0">FORFEO AMBASSADEUR</h5>
            <div class="d-flex align-items-center gap-3">
                <span class="small d-none d-md-inline">Bienvenue, Ambassadeur</span>
                <a href="/logout" class="btn btn-outline-light btn-sm rounded-pill px-3">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row mb-4">
            <div class="col-12">
                <h2 class="fw-bold">Missions disponibles</h2>
                <p class="text-muted">Sélectionnez une mission d'audit et commencez votre expérience VIP.</p>
            </div>
        </div>

        <div class="row g-4">
            <% if (missions.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <div class="card p-5 border-0 shadow-sm rounded-4">
                        <i class="bi bi-search text-muted mb-3" style="font-size: 3rem;"></i>
                        <h4 class="fw-bold">Aucune mission pour le moment</h4>
                        <p class="text-muted">Revenez bientôt pour découvrir de nouvelles opportunités d'audit.</p>
                    </div>
                </div>
            <% } else { %>
                <% missions.forEach(mission => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card mission-card h-100 shadow-sm">
                            <div class="p-4">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <h5 class="fw-bold mb-0"><%= mission.titre %></h5>
                                </div>
                                <p class="small text-muted mb-4 text-truncate-3" style="height: 4.5em; overflow: hidden;">
                                    <%= mission.description %>
                                </p>
                                <div class="mb-4">
                                    <span class="small text-muted d-block mb-1">Votre récompense :</span>
                                    <span class="badge-reward small"><i class="bi bi-gift me-2"></i><%= mission.recompense %></span>
                                </div>
                                <form action="/postuler-mission" method="POST">
                                    <input type="hidden" name="missionId" value="<%= mission.id %>">
                                    <button type="submit" class="btn-apply">POSTULER MAINTENANT</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
</body>
</html>
