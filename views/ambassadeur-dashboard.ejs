<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Missions | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        .card-mission { background: white; border-radius: 15px; padding: 20px; border-left: 5px solid #007bff; margin-bottom: 15px; }
        @media (max-width: 768px) { .navbar-brand { font-size: 1.2rem; } }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-3 py-2 sticky-top">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <div class="d-flex gap-2">
            <a href="/ambassadeur/mes-missions" class="btn btn-primary btn-sm rounded-pill">Mes Missions</a>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Quitter</a>
        </div>
    </nav>

    <div class="container py-5">
        <h2 class="fw-bold mb-1">Missions disponibles au Québec 📍</h2>
        <p class="text-muted small mb-5">Sélectionnez une mission pour commencer votre audit.</p>

        <% if (missions.length === 0) { %>
            <div class="text-center py-5">
                <p class="text-muted">Aucune nouvelle mission disponible.</p>
                <p class="small text-muted">Consultez vos missions en cours dans l'onglet "Mes Missions".</p>
            </div>
        <% } %>

        <div class="row">
            <% missions.forEach(m => { %>
                <div class="col-md-6">
                    <div class="card-mission shadow-sm">
                        <div class="d-flex justify-content-between">
                            <h5 class="fw-bold"><%= m.titre %></h5>
                            <span class="badge bg-success"><%= m.recompense %>$</span>
                        </div>
                        <p class="small text-muted"><%= m.description.substring(0, 100) %>...</p>
                        <form action="/postuler-mission" method="POST">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button type="submit" class="btn btn-primary btn-sm w-100 rounded-pill">Réserver</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
