<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        .card-mission { 
            background: white; 
            border-radius: 15px; 
            padding: 25px; 
            border-left: 5px solid #007bff; 
            transition: 0.3s; 
            height: 100%;
        }
        .card-mission:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .welcome-icon { font-size: 4rem; color: #198754; }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="d-flex align-items-center gap-2">
                <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill px-3">
                    <i class="bi bi-person-circle me-1"></i> Mon Profil
                </a>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">
                    <i class="bi bi-box-arrow-right"></i>
                </a>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <div>
                <h2 class="fw-bold mb-1 text-dark">Gains : <%= totalGains %>$</h2>
                <p class="text-muted">Explorez et réservez des audits mystères au Québec.</p>
            </div>
            <a href="/ambassadeur/mes-missions" class="btn btn-primary rounded-pill px-4 shadow-sm fw-bold">
                Mes Missions
            </a>
        </div>

        <div class="row g-4">
            <% if (missions.length === 0) { %>
                <div class="text-center py-5">
                    <i class="bi bi-search fs-1 text-muted"></i>
                    <p class="mt-3 text-muted">Aucune mission disponible pour le moment.</p>
                </div>
            <% } %>

            <% missions.forEach(m => { %>
                <div class="col-md-6 col-lg-4">
                    <div class="card-mission d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <h5 class="fw-bold text-dark m-0"><%= m.titre %></h5>
                            <span class="badge bg-success-subtle text-success rounded-pill px-3 py-2 fw-bold">
                                <%= m.recompense %>
                            </span>
                        </div>
                        <p class="small text-muted flex-grow-1"><%= m.description %></p>
                        
                        <hr class="my-3">
                        
                        <form action="/postuler-mission" method="POST">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button type="submit" class="btn btn-outline-primary w-100 rounded-pill fw-bold py-2">
                                Réserver cette mission
                            </button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <% if (showWelcome) { %>
    <div class="modal fade" id="welcomeModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow-lg rounded-4">
                <div class="modal-body p-5 text-center">
                    <div class="mb-4">
                        <i class="bi bi-currency-dollar welcome-icon"></i>
                    </div>
                    <h2 class="fw-bold mb-3">Salut <%= userName %> ! 🕵️‍♂️</h2>
                    <p class="text-muted mb-4 lead">
                        Prêt à gagner vos premiers dollars ? Explorez les missions d'audit mystère et commencez à générer des revenus dès aujourd'hui.
                    </p>
                    <button type="button" class="btn btn-primary rounded-pill px-5 py-3 fw-bold shadow" data-bs-dismiss="modal">
                        Découvrir les missions
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var myModal = new bootstrap.Modal(document.getElementById('welcomeModal'));
            myModal.show();
        });
    </script>
    <% } else { %>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <% } %>
</body>
</html>
