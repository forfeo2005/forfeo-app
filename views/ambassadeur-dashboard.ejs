<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Ambassadeur | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body class="bg-light d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="ms-auto d-flex align-items-center gap-3">
                <a href="/" class="nav-link fw-bold text-dark small"><i class="bi bi-house-door"></i> Accueil</a>
                <a href="/profil" class="nav-link fw-bold text-dark small"><i class="bi bi-person-circle"></i> Profil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-3 fw-bold">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h2 class="fw-bold">Gains : <span class="text-success"><%= totalGains %>$</span></h2>
            <a href="/ambassadeur/mes-missions" class="btn btn-primary rounded-pill px-4 fw-bold">Mes Missions</a>
        </div>
        <div class="row g-4">
            <% missions.forEach(m => { %>
                <div class="col-md-4">
                    <div class="card p-4 border-0 shadow-sm rounded-4 h-100">
                        <h5 class="fw-bold mb-3"><%= m.titre %></h5>
                        <form action="/postuler-mission" method="POST">
                            <input type="hidden" name="id_mission" value="<%= m.id %>">
                            <button class="btn btn-outline-primary w-100 rounded-pill fw-bold">Réserver</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer class="bg-dark text-white py-5 mt-auto">
        <div class="container text-center">
            <div class="d-flex justify-content-center gap-3 small mb-3">
                <a href="/politique-confidentialite" class="text-white-50 text-decoration-none">Confidentialité (Loi 25)</a>
                <span class="text-white-25">|</span>
                <a href="/conditions-utilisation" class="text-white-50 text-decoration-none">Conditions d'utilisation</a>
            </div>
            <p class="x-small text-white-50" style="font-size: 0.75rem; opacity: 0.6;">
                <strong>Note :</strong> Vous agissez à titre de <strong>travailleur autonome</strong> indépendant au Québec.
            </p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
