<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Espace Qualité | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f4f7fb; font-family: 'Inter', sans-serif; }
        @media (max-width: 768px) {
            table thead { display: none; }
            table tr { display: block; background: white; margin-bottom: 1rem; border-radius: 15px; padding: 15px; }
            table td { display: flex; justify-content: space-between; border: none !important; padding: 8px 0; }
            table td::before { content: attr(data-label); font-weight: bold; }
        }
    </style>
</head>
<body>
    <nav class="navbar bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        <div class="d-flex align-items-center gap-2">
            <a href="/profil" class="btn btn-outline-primary btn-sm rounded-pill px-3">Profil</a>
            <span class="badge bg-light text-primary rounded-pill">Plan: <%= stats.forfait %></span>
            <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill"><i class="bi bi-box-arrow-right"></i></a>
        </div>
    </nav>

    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="fw-bold mb-0">Votre Espace Qualité 🚀</h3>
            <% if (stats.canPublish) { %>
                <button class="btn btn-primary rounded-pill px-4 fw-bold shadow" data-bs-toggle="modal" data-bs-target="#modalMission">Créer</button>
            <% } else { %>
                <a href="/forfaits" class="btn btn-warning rounded-pill px-3 fw-bold small">Upgrade Premium</a>
            <% } %>
        </div>

        <div class="bg-white rounded-4 p-4 shadow-sm">
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead><tr><th>MISSION</th><th>RÉCOMPENSE</th><th>STATUT</th></tr></thead>
                    <tbody>
                        <% missions.forEach(m => { %>
                            <tr>
                                <td data-label="Mission"><strong><%= m.titre %></strong></td>
                                <td data-label="Prix"><%= m.recompense %></td>
                                <td data-label="Statut"><span class="badge rounded-pill <%= m.statut === 'approuve' ? 'bg-success' : 'bg-warning' %>"><%= m.statut %></span></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <% if (showWelcome) { %>
    <div class="modal fade" id="welcomeModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0 shadow-lg rounded-4">
                <div class="modal-body p-5 text-center">
                    <div class="mb-4"><i class="bi bi-rocket-takeoff text-primary" style="font-size: 4rem;"></i></div>
                    <h2 class="fw-bold mb-3">Bienvenue sur FORFEO, <%= userName %> !</h2>
                    <p class="text-muted mb-4">Améliorez votre expérience client dès aujourd'hui. Publiez votre premier audit mystère et recevez des feedbacks constructifs de nos ambassadeurs qualifiés.</p>
                    <button type="button" class="btn btn-primary rounded-pill px-5 py-2 fw-bold" data-bs-dismiss="modal">Lancer ma première mission</button>
                </div>
            </div>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var myModal = new bootstrap.Modal(document.getElementById('welcomeModal'));
            myModal.show();
        });
    </script>
    <% } %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
