<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord Qualité | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background: #f8fafc; font-family: 'Inter', sans-serif; }
        .navbar { background: white; border-bottom: 1px solid #e2e8f0; }
        .card-table { border: none; border-radius: 20px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); }
        .badge-status { font-weight: 600; padding: 0.5em 1em; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light sticky-top py-3">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary fs-4" href="/">FORFEO LAB</a>
            <div class="ms-auto d-flex align-items-center gap-3">
                <a href="/" class="nav-link fw-bold text-dark small"><i class="bi bi-house-door"></i> Accueil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-4 fw-bold">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h2 class="fw-bold m-0">Tableau de bord Qualité</h2>
            <a href="/forfaits" class="btn btn-warning rounded-pill px-4 fw-bold shadow-sm">Passer au Premium 👑</a>
        </div>

        <div class="card card-table bg-white p-4">
            <h5 class="fw-bold mb-4">Suivi des Audits</h5>
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead class="text-muted small">
                        <tr>
                            <th>MISSION</th>
                            <th>STATUT</th>
                            <th>DATE</th>
                            <th class="text-end">ACTION</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (missions.length === 0) { %>
                            <tr><td colspan="4" class="text-center py-5 text-muted">Aucune mission pour le moment.</td></tr>
                        <% } %>
                        <% missions.forEach(m => { %>
                            <tr>
                                <td class="fw-bold"><%= m.titre %></td>
                                <td>
                                    <% if (m.statut === 'approuve') { %>
                                        <span class="badge rounded-pill bg-success-subtle text-success badge-status">Approuvé</span>
                                    <% } else if (m.statut === 'reserve') { %>
                                        <span class="badge rounded-pill bg-info-subtle text-info badge-status">Réservé</span>
                                    <% } else { %>
                                        <span class="badge rounded-pill bg-warning-subtle text-warning badge-status"><%= m.statut %></span>
                                    <% } %>
                                </td>
                                <td class="text-muted">12/29/2025</td>
                                <td class="text-end">
                                    <% if (m.statut === 'approuve') { %>
                                        <a href="/entreprise/telecharger-rapport/<%= m.id %>" class="btn btn-sm btn-outline-danger rounded-pill px-3 fw-bold">
                                            <i class="bi bi-file-earmark-pdf"></i> Voir rapport
                                        </a>
                                    <% } else { %>
                                        <button class="btn btn-sm btn-light rounded-pill px-3 disabled">Voir rapport</button>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white py-5 mt-auto">
        <div class="container text-center">
            <div class="d-flex justify-content-center gap-3 small mb-3">
                <a href="/politique-confidentialite" class="text-white-50 text-decoration-none">Confidentialité (Loi 25)</a>
                <span class="text-white-25">|</span>
                <a href="/conditions-utilisation" class="text-white-50 text-decoration-none">Conditions d'utilisation</a>
            </div>
            <p class="x-small text-white-50 mb-0" style="font-size: 0.75rem; opacity: 0.6;">
                © 2025 FORFEO LAB | Plateforme conforme aux lois du Québec.
            </p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
