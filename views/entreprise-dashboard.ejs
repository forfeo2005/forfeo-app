<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Portail Entreprise | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        .card-custom { border: none; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); transition: 0.3s; }
        .btn-gradient { background: linear-gradient(135deg, #0d6efd, #00d2ff); border: none; color: white; }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar bg-white border-bottom py-3">
        <div class="container">
            <span class="navbar-brand fw-bold text-primary"><i class="bi bi-building"></i> <%= userName %></span>
            <div><a href="/logout" class="btn btn-danger btn-sm rounded-pill">Déconnexion</a></div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row g-4 mb-5">
            <div class="col-md-4">
                <div class="card card-custom p-4 bg-primary text-white text-center">
                    <h6 class="opacity-75">FORFAIT : <%= user.forfait %></h6>
                    <a href="https://buy.stripe.com/7sY4gAbPs9graUw65Ed7q0a" class="btn btn-light rounded-pill mt-3 fw-bold">Passer au Pro (149$)</a>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card card-custom p-4 bg-white">
                    <h5 class="fw-bold mb-3">Nouvel Audit Mystère</h5>
                    <form action="/entreprise/creer-audit" method="POST" class="row g-3">
                        <div class="col-md-6">
                            <input type="text" name="titre" placeholder="Nom de l'établissement" class="form-control rounded-pill" required>
                        </div>
                        <div class="col-md-6">
                            <select name="type_audit" class="form-select rounded-pill">
                                <option>Audit Standard (Accueil)</option>
                                <option>Audit Propreté & Hygiène</option>
                                <option>Audit Expérience Client Totale</option>
                                <option>Audit Client Mystère Téléphonique</option>
                            </select>
                        </div>
                        <div class="col-md-9">
                            <input type="text" name="recompense" placeholder="Récompense (ex: 50$)" class="form-control rounded-pill">
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">Publier</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="card card-custom p-4 mb-4">
            <h5 class="fw-bold mb-4">Mes Rapports & Missions</h5>
            <table class="table">
                <thead><tr><th>Audit</th><th>Type</th><th>Statut</th><th>PDF</th></tr></thead>
                <tbody>
                    <% missions.forEach(m => { %>
                        <tr>
                            <td><%= m.titre %></td>
                            <td><%= m.type_audit %></td>
                            <td><span class="badge bg-secondary"><%= m.statut %></span></td>
                            <td><a href="/entreprise/telecharger-rapport/<%= m.id %>" class="btn btn-sm btn-outline-dark rounded-pill"><i class="bi bi-file-earmark-pdf"></i> Rapport</a></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <div class="card card-custom p-4">
            <h5 class="fw-bold mb-3">Ajouter un employé</h5>
            <form action="/entreprise/ajouter-employe" method="POST" class="d-flex gap-2">
                <input type="email" name="email" placeholder="Email employé" class="form-control rounded-pill" required>
                <input type="password" name="password" placeholder="Mot de passe" class="form-control rounded-pill" required>
                <button type="submit" class="btn btn-success rounded-pill px-4">Inscrire</button>
            </form>
        </div>
    </div>
</body>
</html>
