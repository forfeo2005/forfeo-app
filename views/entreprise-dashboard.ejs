<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Entreprise | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --primary-blue: #007bff; --gold-premium: #d4af37; }
        
        body { background-color: #f4f7f6; }

        /* Effet de mouvement sur les cartes */
        .hover-card {
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            border: none;
            border-radius: 15px;
        }
        .hover-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
        }

        .premium-status {
            background: linear-gradient(45deg, #1a1a1a, #333);
            color: white;
            border-radius: 15px;
            padding: 20px;
        }

        .btn-premium {
            background: var(--gold-premium);
            color: black;
            font-weight: bold;
            border: none;
            transition: 0.3s;
        }
        .btn-premium:hover {
            transform: scale(1.05);
            background: #b8962d;
        }

        /* Menu Hamburger Mobile */
        @media (max-width: 991px) {
            .navbar-collapse {
                background: white;
                padding: 1rem;
                border-radius: 10px;
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">FORFEO <span class="text-primary">LAB</span></a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navDashboard">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navDashboard">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item"><a class="nav-link" href="/contact">Support</a></li>
                    <li class="nav-item ms-lg-3"><a class="btn btn-outline-light btn-sm rounded-pill" href="/logout">Déconnexion</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        
        <div class="row g-4 mb-5">
            <div class="col-lg-12">
                <div class="premium-status shadow-sm d-flex justify-content-between align-items-center flex-wrap gap-3">
                    <div>
                        <h3 class="fw-bold mb-1">Votre Statut : <%= isPremium ? '💎 Membre Premium' : '⭐ Compte Standard' %></h3>
                        <p class="mb-0 opacity-75">
                            <%= isPremium ? 'Vous avez un accès illimité aux missions et aux ambassadeurs.' : 'Passez au Premium pour publier des missions illimitées.' %>
                        </p>
                    </div>
                    <% if (!isPremium) { %>
                        <div class="d-flex gap-2">
                            <div class="bg-white text-dark p-3 rounded-3 text-center shadow-sm">
                                <small class="d-block fw-bold">MENSUEL</small>
                                <span class="fs-4 fw-bold">49$</span>
                                <a href="/checkout-monthly" class="btn btn-primary btn-sm d-block mt-2 rounded-pill">Choisir</a>
                            </div>
                            <div class="bg-primary text-white p-3 rounded-3 text-center shadow-sm">
                                <small class="d-block fw-bold">ANNUEL</small>
                                <span class="fs-4 fw-bold">499$</span>
                                <a href="/checkout-annual" class="btn btn-light text-primary btn-sm d-block mt-2 rounded-pill fw-bold">Choisir</a>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-lg-5">
                <div class="card hover-card p-4 shadow-sm">
                    <h4 class="fw-bold mb-4">Publier une Mission</h4>
                    <form action="/creer-mission" method="POST">
                        <div class="mb-3">
                            <label class="form-label small fw-bold">Titre de la mission</label>
                            <input type="text" name="titre" class="form-control rounded-pill" placeholder="Ex: Audit Qualité Service" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label small fw-bold">Description des attentes</label>
                            <textarea name="description" class="form-control rounded-4" rows="4" placeholder="Décrivez ce que l'ambassadeur doit tester..." required></textarea>
                        </div>
                        <div class="mb-4">
                            <label class="form-label small fw-bold">Récompense offerte</label>
                            <input type="text" name="recompense" class="form-control rounded-pill" placeholder="Ex: Repas pour 2 personnes offert" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill py-3 fw-bold">LANCER LA MISSION</button>
                    </form>
                </div>
            </div>

            <div class="col-lg-7">
                <h4 class="fw-bold mb-4">Mes Missions Actives</h4>
                <div class="row g-3">
                    <% if (missions.length === 0) { %>
                        <div class="col-12 text-center py-5 bg-white rounded-4 shadow-sm">
                            <p class="text-muted">Vous n'avez pas encore publié de mission.</p>
                        </div>
                    <% } %>
                    <% missions.forEach(mission => { %>
                        <div class="col-12">
                            <div class="card hover-card p-4 shadow-sm border-start border-primary border-4">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div>
                                        <h5 class="fw-bold mb-1"><%= mission.titre %></h5>
                                        <p class="text-muted small mb-2"><%= mission.description %></p>
                                        <span class="badge bg-light text-primary border border-primary rounded-pill">🎁 <%= mission.recompense %></span>
                                    </div>
                                    <span class="badge bg-success rounded-pill">En recherche</span>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
