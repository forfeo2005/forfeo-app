<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><title>Portail Entreprise | FORFEO LAB</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"></head>
<body class="bg-light">
    <nav class="navbar navbar-white bg-white border-bottom py-3 shadow-sm">
        <div class="container">
            <span class="navbar-brand fw-bold text-primary"><i class="bi bi-building"></i> <%= userName %></span>
            <div><a href="/profil" class="btn btn-sm btn-outline-secondary rounded-pill me-2">Profil</a><a href="/logout" class="btn btn-sm btn-danger rounded-pill">Déconnexion</a></div>
        </div>
    </nav>
    <div class="container py-5">
        <div class="row g-4 mb-5">
            <div class="col-md-4">
                <div class="card p-4 border-0 shadow-sm rounded-4 bg-primary text-white">
                    <h6>FORFAIT : <%= user.forfait %></h6>
                    <a href="https://buy.stripe.com/7sY4gAbPs9graUw65Ed7q0a" class="btn btn-light w-100 rounded-pill mt-3 fw-bold">Passer au Pro (149$)</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-4 border-0 shadow-sm rounded-4 h-100">
                    <h6 class="fw-bold mb-3"><i class="bi bi-plus-circle"></i> Créer un Audit</h6>
                    <form action="/entreprise/creer-audit" method="POST">
                        <input type="text" name="titre" placeholder="Titre de l'audit" class="form-control mb-2 rounded-pill shadow-sm" required>
                        <input type="text" name="recompense" placeholder="Récompense (ex: 50$)" class="form-control mb-2 rounded-pill shadow-sm" required>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold shadow-sm">Publier</button>
                    </form>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-4 border-0 shadow-sm rounded-4 h-100">
                    <h6 class="fw-bold mb-3"><i class="bi bi-person-plus"></i> Inscrire un employé</h6>
                    <form action="/entreprise/ajouter-employe" method="POST">
                        <input type="email" name="email" placeholder="Email" class="form-control mb-2 rounded-pill shadow-sm" required>
                        <input type="password" name="password" placeholder="Pass temporaire" class="form-control mb-2 rounded-pill shadow-sm" required>
                        <button type="submit" class="btn btn-success w-100 rounded-pill fw-bold shadow-sm">Inscrire</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="card border-0 shadow-sm rounded-4 p-4 mb-4">
            <h5 class="fw-bold mb-4">Suivi des Formations Employés</h5>
            <table class="table align-middle">
                <thead><tr class="small text-muted"><th>EMPLOYÉ</th><th>SCORE</th><th>STATUT</th></tr></thead>
                <tbody>
                    <% employeesScores.forEach(s => { %>
                        <tr><td class="fw-bold"><%= s.nom_employe %></td><td class="fw-bold"><%= ((s.meilleur_score/15)*100).toFixed(0) %>%</td><td><span class="badge <%= s.meilleur_score >= 12 ? 'bg-success' : 'bg-warning' %> rounded-pill"><%= s.statut %></span></td></tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
