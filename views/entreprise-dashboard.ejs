<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <title>FORFEO LAB - Portail Entreprise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; }
        .dashboard-header { background: white; padding: 20px 0; border-bottom: 1px solid #e2e8f0; }
        .card-mission { background: white; border-radius: 16px; padding: 20px; border: 1px solid #e2e8f0; margin-bottom: 15px; }
        .btn-gradient { background: linear-gradient(45deg, #2563eb, #06b6d4); color: white; border: none; font-weight: bold; border-radius: 12px; }
    </style>
</head>
<body>
    <div class="dashboard-header mb-5 shadow-sm">
        <div class="container d-flex justify-content-between align-items-center">
            <h4 class="fw-bold text-primary mb-0">FORFEO ENTREPRISE</h4>
            <a href="/logout" class="btn btn-outline-danger btn-sm">Déconnexion</a>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="fw-bold">Mes Missions</h2>
                    <button class="btn btn-gradient px-4" data-bs-toggle="modal" data-bs-target="#modalMission">
                        <i class="bi bi-plus-circle me-2"></i> Poster un Audit
                    </button>
                </div>

                <% if (missions.length === 0) { %>
                    <div class="card-mission text-center py-5">
                        <p class="text-muted">Aucune mission publiée pour le moment.</p>
                    </div>
                <% } else { %>
                    <% missions.forEach(mission => { %>
                        <div class="card-mission">
                            <div class="d-flex justify-content-between">
                                <h5 class="fw-bold text-primary"><%= mission.titre %></h5>
                                <span class="badge bg-info"><%= mission.statut %></span>
                            </div>
                            <p class="small text-muted"><%= mission.description %></p>
                            <div class="small fw-bold">🎁 Récompense : <%= mission.recompense %></div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
            
            <div class="col-md-4">
                <div class="card p-4 border-0 shadow-sm rounded-4 bg-white">
                    <h5 class="fw-bold mb-3">Statistiques</h5>
                    <div class="small mb-2 text-muted">Audits réalisés : 0</div>
                    <div class="small mb-2 text-muted">Candidatures en attente : 0</div>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalMission" tabindex="-1">
        <div class="modal-dialog">
            <form action="/creer-mission" method="POST" class="modal-content border-0 rounded-4 shadow">
                <div class="modal-header border-0">
                    <h5 class="fw-bold">Nouvelle Mission d'Audit</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Titre de la mission</label>
                        <input type="text" name="titre" class="form-control" placeholder="Ex: Audit Qualité Restaurant" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Description des tâches</label>
                        <textarea name="description" class="form-control" rows="3" placeholder="Qu'est-ce que l'ambassadeur doit tester ?" required></textarea>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Récompense / Avantage</label>
                        <input type="text" name="recompense" class="form-control" placeholder="Ex: Repas offert (valeur 50$)" required>
                    </div>
                </div>
                <div class="modal-footer border-0">
                    <button type="submit" class="btn btn-gradient w-100 py-2">PUBLIER LA MISSION</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
