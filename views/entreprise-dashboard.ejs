<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Entreprise | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #f4f7f6; }
        .sidebar { background: #1a1a1a; min-height: 100vh; color: white; }
        .card-custom { border: none; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .btn-primary { background: #0d6efd; border-radius: 50px; padding: 10px 25px; }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <nav class="col-md-2 sidebar p-4">
            <h4 class="fw-bold text-primary">FORFEO LAB</h4>
            <hr>
            <a href="#" class="nav-link text-white mb-3 active"><i class="fas fa-th-large me-2"></i> Dashboard</a>
            <a href="/forfaits" class="nav-link text-white mb-3"><i class="fas fa-credit-card me-2"></i> Forfaits (CAD)</a>
            <a href="/logout" class="nav-link text-danger mt-5"><i class="fas fa-sign-out-alt me-2"></i> Déconnexion</a>
        </nav>

        <main class="col-md-10 p-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="fw-bold">Bonjour, <%= userName %> 🏢</h2>
                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#missionModal">
                    <i class="fas fa-plus me-2"></i> Nouvelle Mission d'Audit
                </button>
            </div>

            <div class="row mb-5">
                <div class="col-md-4">
                    <div class="card card-custom p-4 bg-white">
                        <small class="text-muted fw-bold">MISSIONS EN COURS</small>
                        <h2 class="fw-bold mt-2"><%= missions.length %></h2>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card card-custom p-4 bg-primary text-white">
                        <small class="fw-bold">STATUT DU COMPTE</small>
                        <h2 class="fw-bold mt-2">PREMIUM CAD$</h2>
                    </div>
                </div>
            </div>

            <div class="card card-custom p-4">
                <h5 class="fw-bold mb-4">Vos Audits Récents</h5>
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th>Titre</th>
                            <th>Récompense</th>
                            <th>Statut</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% missions.forEach(m => { %>
                            <tr>
                                <td class="fw-bold"><%= m.titre %></td>
                                <td><%= m.recompense %></td>
                                <td><span class="badge bg-success rounded-pill">Actif</span></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>

<div class="modal fade" id="missionModal" tabindex="-1">
    <div class="modal-dialog">
        <form action="/creer-mission" method="POST" class="modal-content border-0 rounded-4 p-3">
            <div class="modal-header border-0">
                <h5 class="fw-bold">Détails de l'Audit Mystère</h5>
            </div>
            <div class="modal-body">
                <input type="text" name="titre" class="form-control mb-3" placeholder="Nom de l'établissement" required>
                <textarea name="description" class="form-control mb-3" placeholder="Consignes pour l'ambassadeur (ex: commander un café et tester l'accueil)"></textarea>
                <input type="text" name="recompense" class="form-control" placeholder="Récompense (ex: Repas offert + 50$ CAD)">
            </div>
            <div class="modal-footer border-0">
                <button type="submit" class="btn btn-primary w-100 shadow">Lancer la mission</button>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/forfy-brain.js"></script>
</body>
</html>
