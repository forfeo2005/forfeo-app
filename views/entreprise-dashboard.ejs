<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .nav-black { background-color: #1a1a1a; padding: 15px; }
        .statut-banner { background-color: #1a1a1a; color: white; border-radius: 20px; padding: 40px; margin-top: 20px; }
        .card-custom { border-radius: 20px; border: none; box-shadow: 0 4px 15px rgba(0,0,0,0.05); background: white; padding: 25px; }
        .mission-item { background: white; border-radius: 15px; border-left: 6px solid #0d6efd; padding: 20px; margin-bottom: 15px; display: flex; justify-content: space-between; align-items: center; }
    </style>
</head>
<body>
    <nav class="nav-black">
        <div class="container d-flex justify-content-between">
            <span class="text-white fw-bold">FORFEO <span class="text-primary">LAB</span></span>
            <a href="/logout" class="btn btn-outline-light btn-sm rounded-pill">Déconnexion</a>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="statut-banner shadow">
            <h2>Votre Statut : 💎 Membre Premium</h2>
            <p class="opacity-75">Bienvenue <%= userName %>, vous avez un accès illimité.</p>
        </div>

        <div class="row mt-5">
            <div class="col-lg-4">
                <div class="card-custom">
                    <h5 class="fw-bold mb-4">Lancer une mission</h5>
                    <form action="/creer-mission" method="POST">
                        <input type="text" name="titre" class="form-control rounded-pill mb-3" placeholder="Titre" required>
                        <textarea name="description" class="form-control mb-3" rows="4" style="border-radius:15px;" placeholder="Description" required></textarea>
                        <input type="text" name="recompense" class="form-control rounded-pill mb-4" placeholder="Récompense" required>
                        <button type="submit" class="btn btn-primary w-100 rounded-pill fw-bold">PUBLIER</button>
                    </form>
                </div>
            </div>
            <div class="col-lg-8">
                <h5 class="fw-bold mb-4">Missions Actives</h5>
                <% missions.forEach(m => { %>
                    <div class="mission-item shadow-sm">
                        <div>
                            <h6 class="fw-bold mb-1"><%= m.titre %></h6>
                            <small class="text-muted"><%= m.recompense %></small>
                        </div>
                        <span class="badge bg-success rounded-pill px-3">Actif</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

    <div id="forfy-btn" style="position:fixed; bottom:20px; right:20px; cursor:pointer;">
        <img src="/ForfyLogo.png" style="width:60px;">
    </div>
    <div id="forfy-chat" style="display:none; position:fixed; bottom:90px; right:20px; width:300px; background:white; border-radius:15px; border:1px solid #ddd; overflow:hidden;">
        <div style="background:#0d6efd; color:white; padding:10px; font-weight:bold;">Forfy - Assistant</div>
        <div id="forfy-msgs" style="height:300px; padding:15px; overflow-y:auto; font-size:0.9rem;"></div>
        <div style="padding:10px; border-top:1px solid #eee;">
            <input type="text" id="forfy-input" class="form-control rounded-pill" placeholder="Posez une question...">
        </div>
    </div>

    <script src="/js/forfy-brain.js"></script>
</body>
</html>
