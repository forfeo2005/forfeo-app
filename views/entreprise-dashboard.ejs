<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Entreprise | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
</head>
<body class="bg-light d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="ms-auto d-flex align-items-center gap-3">
                <a href="/" class="nav-link fw-bold text-dark small"><i class="bi bi-house-door"></i> Accueil</a>
                <a href="/profil" class="nav-link fw-bold text-dark small"><i class="bi bi-person-circle"></i> Profil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-3 fw-bold">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h2 class="fw-bold m-0">Espace Entreprise 🚀</h2>
            <div class="d-flex gap-2">
                <a href="/entreprise/statistiques" class="btn btn-outline-primary rounded-pill px-3 fw-bold">Analyses 📊</a>
                <a href="/forfaits" class="btn btn-warning rounded-pill px-3 fw-bold shadow-sm">Upgrade Premium</a>
            </div>
        </div>

        <div class="card p-4 border-0 shadow-sm rounded-4 bg-white">
            <h5 class="fw-bold mb-4">Mes Audits Mystères</h5>
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead class="table-light"><tr><th>Mission</th><th>Statut</th><th class="text-end">Rapport</th></tr></thead>
                    <tbody>
                        <% if (missions.length === 0) { %><tr><td colspan="3" class="text-center py-4">Aucune mission publiée.</td></tr><% } %>
                        <% missions.forEach(m => { %>
                            <tr>
                                <td class="fw-bold"><%= m.titre %></td>
                                <td><span class="badge bg-light text-primary border border-primary"><%= m.statut %></span></td>
                                <td class="text-end"><a href="/entreprise/telecharger-rapport/<%= m.id %>" class="btn btn-sm btn-outline-danger">PDF</a></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    </body>
</html>
