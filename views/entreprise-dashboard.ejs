<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Entreprise | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
            <div class="ms-auto d-flex align-items-center gap-3">
                <a href="/" class="nav-link fw-bold text-dark small">Accueil</a>
                <a href="/logout" class="btn btn-danger btn-sm rounded-pill px-4 fw-bold">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="container py-5 flex-grow-1">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h1 class="fw-bold">Tableau de bord Qualité</h1>
            <a href="/forfaits" class="btn btn-warning fw-bold rounded-pill px-4">Passer au Premium 👑</a>
        </div>

        <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
            <div class="card-header bg-white py-3"><h5 class="mb-0 fw-bold">Suivi des Audits</h5></div>
            <table class="table align-middle mb-0">
                <thead class="bg-light">
                    <tr><th>Mission</th><th>Statut</th><th>Date</th><th>Action</th></tr>
                </thead>
                <tbody>
                    <% missions.forEach(m => { %>
                    <tr>
                        <td class="fw-bold"><%= m.titre %></td>
                        <td><span class="badge bg-info rounded-pill"><%= m.statut %></span></td>
                        <td class="text-muted"><%= new Date().toLocaleDateString() %></td>
                        <td><button class="btn btn-sm btn-light border rounded-pill">Voir rapport</button></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
