<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Entreprise | FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .mission-card {
            transition: all 0.3s ease;
            border-radius: 15px;
            border: 1px solid #eee;
        }
        /* Effet qui bouge */
        .mission-card:hover {
            transform: scale(1.02) translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
        }
        .premium-badge {
            background: linear-gradient(45deg, #ffd700, #ffa500);
            color: black; font-weight: bold; border-radius: 20px; padding: 5px 15px;
        }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#">FORFEO <span class="text-primary">PRO</span></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navDash">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navDash">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/logout">Déconnexion</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
            <h2 class="fw-bold">Mon Espace</h2>
            <% if (isPremium) { %>
                <span class="premium-badge">💎 MEMBRE PREMIUM</span>
            <% } else { %>
                <a href="/checkout" class="btn btn-warning fw-bold rounded-pill shadow-sm">Passer au Premium</a>
            <% } %>
        </div>

        <div class="row g-4">
            <div class="col-lg-4">
                <div class="card p-4 shadow-sm border-0 rounded-4">
                    <h4 class="fw-bold mb-3">Nouvelle Mission</h4>
                    <form action="/creer-mission" method="POST">
                        <input type="text" name="titre" class="form-control mb-3" placeholder="Titre (ex: Audit Accueil)" required>
                        <textarea name="description" class="form-control mb-3" placeholder="Instructions..." required></textarea>
                        <input type="text" name="recompense" class="form-control mb-3" placeholder="Récompense (ex: Repas offert)">
                        <button type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold">Publier la mission</button>
                    </form>
                </div>
            </div>

            <div class="col-lg-8">
                <h4 class="fw-bold mb-3">Missions en cours</h4>
                <div class="row g-3">
                    <% missions.forEach(mission => { %>
                        <div class="col-12 col-md-6">
                            <div class="mission-card bg-white p-3 shadow-sm h-100">
                                <h5 class="fw-bold text-primary"><%= mission.titre %></h5>
                                <p class="small text-muted mb-2"><%= mission.description %></p>
                                <span class="badge bg-light text-dark border"><%= mission.recompense %></span>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
