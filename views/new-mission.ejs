<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <title>Nouvelle Mission - FORFEO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">
    <div class="card border-0 shadow-lg p-4 rounded-4" style="max-width: 600px; width: 100%;">
        <h3 class="fw-bold mb-4">Commander une nouvelle mission</h3>
        
        <form action="/new-mission" method="POST">
            <input type="hidden" name="userId" value="<%= userId %>">

            <div class="mb-3">
                <label class="form-label fw-bold">Type de mission</label>
                <select name="type" class="form-select form-select-lg bg-light border-0" required>
                    <option value="Audit Qualité (Client Mystère)">Audit Qualité (Client Mystère)</option>
                    <option value="Audit Expérience Client">Audit Expérience Client</option>
                    <option value="Formation Équipe">Formation Équipe</option>
                    <option value="Consultation Stratégique">Consultation Stratégique</option>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Date souhaitée</label>
                <input type="date" name="date" class="form-control form-control-lg bg-light border-0" required>
            </div>

            <div class="mb-4">
                <label class="form-label fw-bold">Détails ou instructions spéciales</label>
                <textarea name="details" class="form-control bg-light border-0" rows="4" placeholder="Ex: Vérifier l'accueil du matin en priorité..."></textarea>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg rounded-pill">Valider la commande</button>
                <a href="/dashboard?id=<%= userId %>" class="btn btn-outline-secondary rounded-pill">Annuler</a>
            </div>
        </form>
    </div>
</body>
</html>
