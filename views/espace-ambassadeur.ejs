<!DOCTYPE html>
<html lang="fr-CA">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FORFEO LAB - Inscription Ambassadeur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        body { background-color: #f1f5f9; font-family: 'Plus Jakarta Sans', sans-serif; display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
        .signup-card { width: 100%; max-width: 450px; background: white; padding: 40px; border-radius: 24px; box-shadow: 0 10px 30px rgba(0,0,0,0.05); border: 1px solid #e2e8f0; }
        .btn-amb { background: linear-gradient(45deg, #2563eb, #06b6d4); color: white; border: none; border-radius: 12px; padding: 12px; font-weight: bold; width: 100%; }
        .form-control { border-radius: 10px; padding: 12px; background: #f8fafc; border: 1px solid #e2e8f0; }
    </style>
</head>
<body>
    <div class="signup-card">
        <div class="text-center mb-4">
            <h2 class="fw-bold text-primary">AMBASSADEUR</h2>
            <p class="text-muted">Devenez membre VIP et testez des expériences exclusives.</p>
        </div>
        <form action="/signup-ambassadeur" method="POST">
            <div class="mb-3">
                <label class="form-label small fw-bold">Nom complet</label>
                <input type="text" name="nom" class="form-control" placeholder="Jean Tremblay" required>
            </div>
            <div class="mb-3">
                <label class="form-label small fw-bold">Courriel personnel</label>
                <input type="email" name="email" class="form-control" placeholder="jean.t@gmail.com" required>
            </div>
            <div class="mb-3">
                <label class="form-label small fw-bold">Ville de résidence</label>
                <input type="text" name="ville" class="form-control" placeholder="Ex: Québec" required>
            </div>
            <div class="mb-4">
                <label class="form-label small fw-bold">Mot de passe</label>
                <input type="password" name="password" class="form-control" required>
            </div>
            <button type="submit" class="btn-amb shadow-sm">REJOINDRE LE CERCLE</button>
            <div class="text-center mt-4 small">
                Déjà membre ? <a href="/login" class="text-primary fw-bold text-decoration-none">Se connecter</a>
            </div>
        </form>
    </div>
</body>
</html>
