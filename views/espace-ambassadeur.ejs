<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rejoindre les Ambassadeurs | FORFEO LAB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .amb-card { border-radius: 20px; border-left: 5px solid #007bff; transition: all 0.3s ease; }
        .amb-card:hover { transform: scale(1.02); box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary" href="/">FORFEO LAB</a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="amb-card bg-white shadow-lg p-5">
                    <h2 class="fw-bold text-center mb-2">Candidature Ambassadeur</h2>
                    <p class="text-center text-muted mb-4 small">Rejoignez l'élite des auditeurs mystères.</p>
                    <form action="/register" method="POST">
                        <input type="hidden" name="role" value="ambassadeur">
                        <div class="mb-3">
                            <label class="form-label">Nom Complet</label>
                            <input type="text" name="nom" class="form-control rounded-pill px-4" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Votre Ville d'action</label>
                            <input type="text" name="ville" class="form-control rounded-pill px-4" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Email</label>
                            <input type="email" name="email" class="form-control rounded-pill px-4" required>
                        </div>
                        <div class="mb-4">
                            <label class="form-label">Mot de passe</label>
                            <input type="password" name="password" class="form-control rounded-pill px-4" required>
                        </div>
                        <button type="submit" class="btn btn-dark w-100 rounded-pill py-3 fw-bold">POSTULER AU CERCLE</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
