<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Évaluation Qualité | Forfeo Lab</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .survey-container { max-width: 800px; margin: 50px auto; padding: 40px; background: white; border-radius: 15px; shadow: 0 10px 30px rgba(0,0,0,0.1); border: 1px solid #eee; }
        .question { margin-bottom: 30px; }
        .question label { display: block; font-weight: bold; margin-bottom: 10px; font-size: 1.1rem; }
        input[type="range"] { width: 100%; margin: 10px 0; }
        textarea { width: 100%; padding: 10px; border-radius: 5px; border: 1px solid #ddd; height: 100px; }
        .btn-submit { background: #000; color: white; padding: 15px 30px; border: none; border-radius: 5px; cursor: pointer; font-weight: bold; width: 100%; }
    </style>
</head>
<body style="background:#f9f9f9; font-family: sans-serif;">
    <div class="survey-container">
        <a href="/dashboard" style="text-decoration:none; color:#666;">← Retour au tableau de bord</a>
        <h1 style="margin-top:20px;">🔍 Analyse de la Qualité de Service</h1>
        <p style="color:#666; margin-bottom:40px;">Évaluez objectivement les points clés de l'expérience opérationnelle.</p>

        <form action="/submit-survey" method="POST">
            <input type="hidden" name="type" value="qualite">
            
            <div class="question">
                <label>1. Propreté et présentation des lieux (1 à 10) :</label>
                <input type="range" min="1" max="10" name="proprete" oninput="this.nextElementSibling.value = this.value">
                <output>5</output>
            </div>

            <div class="question">
                <label>2. Rapidité de la prise en charge initiale :</label>
                <select name="vitesse" style="width:100%; padding:10px;">
                    <option value="immediat">Immédiate</option>
                    <option value="correct">Acceptable (moins de 5 min)</option>
                    <option value="lent">Lente (plus de 5 min)</option>
                </select>
            </div>

            <div class="question">
                <label>3. Observations spécifiques sur la conformité aux standards :</label>
                <textarea name="observations" placeholder="Détaillez vos observations ici..."></textarea>
            </div>

            <button type="submit" class="btn-submit">ENREGISTRER L'ÉVALUATION</button>
        </form>
    </div>

    <%- include('partials/forfy') %>
</body>
</html>
